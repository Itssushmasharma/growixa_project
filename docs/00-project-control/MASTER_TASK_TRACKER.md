# Master Task Tracker

- Document ID: DOC-TASK-TRACKER
- Status: ACTIVE
- Version: 1.3
- Last updated: 2026-07-31
- Owner: Coding agent
- Related documents: [SPRINT_01_FOUNDATION](../14-sprints/SPRINT_01_FOUNDATION.md), [SPRINT_02_CONTACTS](../14-sprints/SPRINT_02_CONTACTS.md), [DEVELOPMENT_READINESS](DEVELOPMENT_READINESS.md), [FEATURE_STATUS_MATRIX](FEATURE_STATUS_MATRIX.md)

Statuses: `BACKLOG`, `READY`, `IN_PROGRESS`, `BLOCKED`, `IN_REVIEW`, `TESTING`, `DONE`,
`DEFERRED`, `CANCELLED`. Priorities: `P0`, `P1`, `P2`, `P3`.

No task below `READY` may be started (per [AGENT_EXECUTION_RULES.md](../12-development/AGENT_EXECUTION_RULES.md)).
A task becomes `READY` only after its dependencies are `DONE` and the
[Development Readiness Gate](DEVELOPMENT_READINESS.md) passes for its slice.

| Task ID | Epic | Task | Description | Priority | Status | Dependencies | Assigned Agent | Files/Modules | Acceptance Criteria | Required Tests | Blocker | Started At | Completed At | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GRX-DOC-001 | Documentation | Establish Growixa documentation foundation | Phase 0/1 docs: product vision, PRD, MVP scope, roadmap, future-scope mapping, decisions, assumptions, open questions, DoD, agent execution rules | P0 | DONE | — | coding agent | `docs/00-project-control/`, `docs/01-product/`, `docs/12-development/` | Docs exist, are internally consistent, and readiness items they cover show PASS | N/A (documentation) | — | 2026-07-22 | 2026-07-22 | Commit `50b93cf` |
| GRX-DOC-002 | Documentation | Architecture, data, and security baseline docs | SYSTEM_ARCHITECTURE, MODULE_BOUNDARIES, BACKGROUND_JOB_ARCHITECTURE, DATA_MODEL, ERD, DATABASE_SCHEMA, SECURITY_ARCHITECTURE, AUTHENTICATION, RBAC, THREAT_MODEL | P0 | DONE | GRX-DOC-001 | coding agent | `docs/04-architecture/`, `docs/05-data/`, `docs/08-security/` | Development Readiness Gate items for these areas show PASS | N/A (documentation) | — | 2026-07-22 | 2026-07-22 | This commit |
| GRX-FOUND-001 | Foundation | Repository and development tooling | Backend/frontend project scaffolding, linting, formatting, type-checking config, pre-commit hooks, env var examples | P0 | DONE | GRX-DOC-002 | coding agent | `apps/api/` (pyproject.toml, .venv tooling, .env.example), `apps/web/` (package.json, eslint/prettier/tsconfig, .env.example), `.pre-commit-config.yaml`, `.gitignore` | Lint/format/type-check run clean on empty scaffolding; `.env.example` present | Config validation only | — | 2026-07-23 | 2026-07-23 | `ruff check .`→0 errors; `ruff format --check .`→pass; `mypy .`→0 issues; `npm run lint`→pass (0 errors); `npm run format:check`→pass; `npm run typecheck`→pass; `npm audit`→0 vulnerabilities; `pre-commit run --all-files`→all hooks pass. Commit `42f8b37` |
| GRX-FOUND-002 | Foundation | Docker Compose local environment | Compose file for Postgres, Redis, RabbitMQ, backend, frontend | P0 | DONE | GRX-FOUND-001 (DONE) | coding agent | `compose.yaml`, `apps/api/Dockerfile`, `apps/api/src/growixa_api/{main,config}.py`, `apps/web/Dockerfile`, `apps/web/next.config.mjs`, `apps/web/src/app/`, root `.env.example` | `docker compose up` starts all services; backend/frontend reach DB/Redis/RabbitMQ | Manual + smoke test | — | 2026-07-23 | 2026-07-23 | `podman compose up -d` → all 5 containers (`postgres`, `redis`, `rabbitmq`, `api`, `web`) report `healthy`; `GET /health` → `{"status":"ok","checks":{"postgres":"ok","redis":"ok","rabbitmq":"ok"}}`; `GET /` on web → 200. Commit `76354d2` |
| GRX-FOUND-003 | Foundation | FastAPI application foundation | App factory, config loading, health endpoint, OpenAPI docs enabled | P0 | DONE | GRX-FOUND-002 (DONE) | coding agent | `apps/api/src/growixa_api/{app,health,main}.py`, `apps/api/tests/test_health.py` | `GET /health` returns 200 locally and in Docker Compose | Unit test for health endpoint | — | 2026-07-23 | 2026-07-23 | `ruff check` 0 errors, `ruff format --check` pass, `mypy` 0 issues, `pytest` 2 passed; `podman compose up -d` → all 5 healthy; `GET /health` → 200 `{"status":"ok",...}`; `GET /docs`, `/openapi.json` → 200. Commit `c69eb10` |
| GRX-FOUND-004 | Foundation | Next.js application foundation | App shell, routing baseline, API client setup, env config | P0 | DONE | GRX-FOUND-002 (DONE) | coding agent | `apps/web/src/app/{page,not-found}.tsx`, `apps/web/src/lib/{env,api-client}.ts` | App builds and serves a placeholder page locally and in Docker Compose | Build passes; smoke test loads root route | — | 2026-07-24 | 2026-07-24 | `npm run lint`/`format:check`/`typecheck`/`build` all pass; local `next start` → `/` 200 (renders "Growixa"), unknown route → 404; `podman compose up -d --build web` → all 5 healthy, `GET /` → 200, unknown route → 404. Commit `8269436` |
| GRX-FOUND-005 | Foundation | PostgreSQL connectivity + Alembic foundation | SQLAlchemy engine/session setup, Alembic init, first empty migration runs | P0 | DONE | GRX-FOUND-003 (DONE) | coding agent | `apps/api/src/growixa_api/db.py`, `apps/api/alembic.ini`, `apps/api/migrations/`, `apps/api/tests/test_migrations.py` | `alembic upgrade head` succeeds against Compose Postgres | Migration smoke test | — | 2026-07-24 | 2026-07-24 | `ruff check` 0 errors, `ruff format --check` pass, `mypy` 0 issues (11 files), `pytest` 3 passed incl. real upgrade/downgrade round-trip; `alembic upgrade head`/`alembic current` succeed both from host (→ localhost:5433) and via `podman compose exec api alembic upgrade head` against Compose Postgres. Commit `0cff500` |
| GRX-FOUND-006 | Foundation | Redis connectivity | Redis client wiring, used later for rate limiting/locks/idempotency | P0 | DONE | GRX-FOUND-003 (DONE) | coding agent | `apps/api/src/growixa_api/redis.py` (new), `apps/api/src/growixa_api/health.py` (reuses pooled client), `apps/api/tests/test_redis.py` | Backend can set/get a key against Compose Redis | Connectivity smoke test | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 38 passed, 1 skipped (95% coverage) — the new Redis smoke test skips under host-run pytest because Compose's `redis` service deliberately has no host port mapping (documented in `LOCAL_DEVELOPMENT.md`; only Postgres/RabbitMQ are host-reachable), a documented environment fact, not a code defect; rebuilt `api` image, `GET /health` → `{"status":"ok",...}` using the new pooled client (replacing health.py's old per-request throwaway client), and a live set/get/delete against real Compose Redis executed inside the `api` container via the shared client confirmed round-trip correctness. Commit `32aacdf` |
| GRX-FOUND-007 | Foundation | RabbitMQ connectivity and health check | Producer/consumer connection setup, `grx.system.healthcheck` queue and worker skeleton per [BACKGROUND_JOB_ARCHITECTURE.md](../04-architecture/BACKGROUND_JOB_ARCHITECTURE.md) | P0 | DONE | GRX-FOUND-003 (DONE) | coding agent | `apps/api/src/growixa_api/jobs/` (new), `apps/worker/` (new) | Worker consumes a health-check message published by the API | Connectivity smoke test | — | 2026-07-29 | 2026-07-29 | Added the shared `JobEnvelope` schema (`jobs/schemas.py`) and `publish_job()` producer (`jobs/producer.py`) to `apps/api`, plus an `admin.access`-gated `POST /system/jobs/healthcheck` to trigger one for testability (no business-job endpoint exists in Sprint 1 otherwise). New standalone `apps/worker/` app (own pyproject.toml/Dockerfile, mirrors `apps/api`'s tooling) connects to RabbitMQ, declares the `grx.system.healthcheck` queue, and logs each job it processes — an independently deployable process per SYSTEM_ARCHITECTURE.md, not a thread inside the API. Wired into `compose.yaml` as a new `worker` service and into `ci.yml` as a new parallel job (its own test suite is pure-unit, no service containers needed). `ruff`/`mypy` clean on both apps; `apps/api` `pytest` 58 passed, 3 skipped (95% coverage) — the new RabbitMQ producer round-trip test skips locally for the same host-unreachable-broker reason as `GRX-FOUND-006`'s Redis test, but runs for real in CI; `apps/worker` `pytest` 2 passed. Live-verified against rebuilt Compose containers: `docker compose logs worker` showed the worker listening on the queue; triggered a real healthcheck job as a smoke Admin via curl, confirmed the worker's log line showed the exact same `job_id`; confirmed a Viewer gets 403; `/health`'s existing `rabbitmq` check still reports `ok`. Also corrected a stale placeholder in `LOCAL_DEVELOPMENT.md` (previously said the worker would build from `apps/api/`). Commit `a0a1eea` |
| GRX-AUDIT-001 | Audit | Audit log module | `audit_logs` table/migration, insert-only repository/service, structured-log redaction of sensitive fields | P0 | DONE | GRX-FOUND-005 (DONE), GRX-AUTH-001 (DONE) | coding agent | `apps/api/src/growixa_api/audit/{models,repositories,services}.py`, migration `6575d09949f9`, `apps/api/tests/{test_audit_log,test_audit_insert_only}.py` | Audit events can be written and listed; no UPDATE/DELETE path exists | Unit tests for write/list; negative test for missing update/delete routes | — | 2026-07-25 | 2026-07-25 | `ruff`/`mypy` clean; `pytest` 14 passed incl. write/list round-trip, system-actor (NULL) event, sensitive-metadata redaction, and a negative test proving no update/delete function exists in `audit.repositories`/`audit.services`; `podman compose exec api alembic current` → head; `/health` unaffected. Commit `2b1dd7e` |
| GRX-AUTH-001 | Auth | Users, roles, permissions schema + seed | `users`, `roles`, `permissions`, `role_permissions`, `user_roles` tables/migrations, Sprint 1 seed data per [RBAC.md](../08-security/RBAC.md) | P0 | DONE | GRX-FOUND-005 (DONE) | coding agent | `apps/api/src/growixa_api/{roles,permissions,users}/models.py`, migration `d330e8b64b48`, `apps/api/tests/test_auth_schema_seed.py` | Migration seeds 6 roles and Sprint 1 permission codes | Migration test | — | 2026-07-24 | 2026-07-24 | `ruff check`/`format --check`/`mypy` clean; `pytest` 4 passed incl. seed-data assertion (6 roles, 6 permission codes, exact 16-row role→permission matrix per RBAC.md) and full upgrade/downgrade round-trip; verified directly via `psql` and via `podman compose exec api alembic current` → head. Commit `daf9bc7` |
| GRX-AUTH-002 | Auth | Password hashing + login/logout | Argon2id hashing, login endpoint, access/refresh cookie issuance, logout | P0 | DONE | GRX-AUTH-001 (DONE), GRX-AUDIT-001 (DONE) | coding agent | `apps/api/src/growixa_api/auth/{models,security,tokens,repositories,services,schemas,api}.py`, migration `ea25a5343142`, `apps/api/src/growixa_api/users/repositories.py`, `apps/api/tests/test_auth_login.py` | Valid login issues cookies and `user.login` audit event; invalid login returns generic error and `user.login_failed` event | Unit + integration tests incl. failure path | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 23 passed (94% coverage — corrected from a coverage.py+greenlet tracing bug found and fixed in this task) incl. valid login (cookies, no tokens in body, audit event), identical generic error for wrong-password/unknown-email, disabled-account rejection, logout (revokes token, clears cookies, audit event); rebuilt `api` image, `alembic current` → head; live curl flow against Compose confirmed cookie attributes and behavior. Commit `b7cf4d8` |
| GRX-AUTH-003 | Auth | Refresh-token rotation + session revocation | Rotating refresh tokens, reuse detection, logout-all-sessions, account disable revokes sessions | P0 | DONE | GRX-AUTH-002 (DONE) | coding agent | `apps/api/src/growixa_api/auth/{repositories,services,api}.py` (extended), `apps/api/tests/test_auth_refresh.py` | Refresh rotates on use; reused old token revokes chain; disable revokes sessions | Integration tests for rotation, reuse detection, disable | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 28 passed (94% coverage) incl. rotation (new cookies, old token revoked), reuse detection (revokes entire chain, `session.revoked` audit event), `logout-all` (revokes every session across simulated devices), disable-revokes-sessions (via the reusable `revoke_all_active_sessions()`), and expired-token rejection; rebuilt `api` image; live curl rotation+reuse flow against Compose confirmed both tokens revoked and audit event recorded via direct DB query. Commit `27a22af` |
| GRX-AUTH-004 | Auth | Login rate limiting | Redis-backed rate limit on login + password-reset-request endpoints (T1/T12 in [THREAT_MODEL.md](../08-security/THREAT_MODEL.md)) | P0 | DONE | GRX-AUTH-002 (DONE), GRX-FOUND-006 (DONE) | coding agent | `apps/api/src/growixa_api/auth/rate_limit.py` (new), `apps/api/src/growixa_api/auth/api.py` (login + password-reset-request routes extended), `apps/api/src/growixa_api/config.py` (`rate_limit_max_attempts`/`rate_limit_window_seconds`), `apps/api/tests/test_auth_rate_limit.py` | Exceeding threshold returns 429; legitimate traffic unaffected | Unit test for limiter logic | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 43 passed, 2 skipped (95% coverage) incl. 5 pure unit tests against a fake Redis stand-in (allows up to max, raises past max, isolates by identifier, isolates by bucket, fails open on `RedisError`) plus an integration test that skips under host-run pytest for the same documented Redis-unreachable-from-host reason as `GRX-FOUND-006`; rebuilt `api` image and verified live against real Compose Redis: 6 wrong-password attempts against one email+IP returned `401×5` then `429`, a correct password against that same already-limited identifier also correctly returned `429` (rate limiting is identity-keyed, not correctness-keyed — this is the intended brute-force defense, not a bug), a different email+IP logged in normally (`200`, proving legitimate traffic elsewhere is unaffected), and the same 5-then-429 pattern was confirmed independently on `/auth/password-reset/request`; verified via `redis-cli KEYS "grx:ratelimit:*"` that keys use the exact `grx:ratelimit:{bucket}:{identifier}` naming `LOCAL_DEVELOPMENT.md` documents, then cleaned up. Design note: the limiter fails open (allows the request) on any `RedisError` so a Redis outage degrades security rather than availability — an explicit, flagged engineering call, not a gap; see CHANGELOG. Commit `5c26200` |
| GRX-AUTH-005 | Auth | Password reset flow | Request + completion endpoints, hashed single-use tokens | P0 | DONE | GRX-AUTH-002 (DONE), GRX-AUDIT-001 (DONE) | coding agent | `apps/api/src/growixa_api/auth/{models,repositories,services,schemas,api}.py` (extended), `apps/api/src/growixa_api/config.py` (`password_reset_ttl_minutes`), migration `bb25de08ba84`, `apps/api/tests/test_auth_password_reset.py` | Reset completes, invalidates token, revokes existing sessions, emits audit events | Integration tests | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 38 passed (95% coverage) incl. full request→complete round-trip (old password rejected, new password works, active sessions revoked, `user.password_reset_requested`/`user.password_reset_completed`/`session.revoked` all emitted), identical generic response for known vs. unknown email, and invalid/reused-token rejection; also verified the response's raw `token` field is `null` for both known and unknown email once `ENVIRONMENT != local` (a monkeypatched-settings test), confirming THREAT_MODEL.md T11 holds outside local dev; rebuilt `api` image, `alembic current` → head; live curl request→complete flow against Compose confirmed via direct DB query (audit events, session revocation, token `used_at`). Interim scope note: the raw token is only ever returned to the caller when `settings.environment == "local"` (same pattern as GRX-USER-001's invitation token) — no email-delivery channel exists yet in Sprint 1. Commit `730519b` |
| GRX-USER-001 | User Management | Internal user invitation + acceptance | Admin invites by email + role; invitee accepts via token | P0 | DONE | GRX-AUTH-001 (DONE), GRX-AUDIT-001 (DONE) | coding agent | `apps/api/src/growixa_api/users/{models,repositories,services,schemas,api}.py`, `apps/api/src/growixa_api/roles/repositories.py`, migration `f356da0136c3`, `apps/api/tests/test_users_invitations.py` | Invitation created, accepted, user provisioned with assigned role, `invitation.accepted` emitted | Integration tests | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 34 passed (94% coverage) incl. full invite→accept round-trip (role assigned, `invitation.accepted` audited), non-admin 403, unknown-role 400, duplicate-email 409, invalid/already-accepted token 400; rebuilt `api` image, `alembic current` → head; live curl invite+accept flow against Compose confirmed role assignment and audit event via direct DB query. Raw invitation token is returned in the create-invitation API response (no email-delivery channel exists in Sprint 1 to send it — flagged, not silently done) — see CHANGELOG. Commit `87d2110` |
| GRX-USER-002 | User Management | User management screens (list, invite, disable, role assignment) | Admin-facing user list/detail UI | P1 | DONE | GRX-USER-001 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/dashboard/team/`, `apps/api/src/growixa_api/{users,roles}/` (extended) | Admin can invite, view, disable a user and change role through the UI | Frontend component tests + one e2e happy path | — | 2026-07-27 | 2026-07-29 | Added `GET /roles` (gated on `users.manage`, not `roles.manage` — it only backs the role-picker dropdown), `GET /users` (list with roles/status), `PATCH /users/{id}/status` and `/role`. Self-disable is blocked (`SelfActionNotAllowedError`) to avoid an unrecoverable lockout with no `seed_first_admin` CLI yet; disabling revokes all active sessions; role changes emit a `role.changed` audit event with old/new roles. New Team page (`/dashboard/team`) lists members, invites, changes roles, and disables/enables, gated on `users.manage`; sidebar's Team link only shows for users with that permission. `ruff`/`mypy` clean, `pytest` 55 passed (95% coverage, 10 new); frontend lint/format/typecheck/build clean, Vitest 14 passed (5 new), Playwright 4 passed (1 new e2e: invite → accept → login happy path). Live-verified in a real browser against rebuilt Compose containers: invited a user, changed their role, disabled them (session revoked, login now 401), confirmed self-disable is blocked in the UI. This closes out the last tracked Sprint 1 task. Commit `22ba450` |
| GRX-RBAC-001 | RBAC | Centralized permission-check dependency | `require_permission()` FastAPI dependency wired to every non-public route | P0 | DONE | GRX-AUTH-001 (DONE) | coding agent | `apps/api/src/growixa_api/permissions/{repositories,dependencies}.py`, `apps/api/tests/test_require_permission.py`, `apps/api/tests/test_protected_routes_audit.py` | A user lacking a permission gets 403 on a protected route in a test | Unit + integration tests, incl. negative case | — | 2026-07-24 | 2026-07-24 | `ruff`/`mypy` clean; `pytest` 9 passed (allowed/403/401-missing/401-invalid cases using the real GRX-AUTH-001-seeded Viewer role, plus a route-protection audit over `create_app()`); rebuilt `api` image, `/health` unaffected. Commit `7e77444` |
| GRX-COMPANY-001 | Company Settings | Company profile + brand settings | `company_profile`/`brand_profiles` tables, edit/view endpoints, RBAC-gated | P0 | DONE | GRX-FOUND-005 (DONE), GRX-RBAC-001 (DONE) | coding agent | `apps/api/src/growixa_api/{company,brand}/{models,schemas,repositories,services,api}.py`, migration `1abf62872712`, `apps/api/tests/test_company_settings.py` | Admin can save company settings; Viewer can view but not edit | Integration tests for edit/view permission split | — | 2026-07-25 | 2026-07-25 | `ruff`/`mypy` clean; `pytest` 19 passed (86% coverage) incl. admin edit+view, viewer view-only (403 on edit) for both company and brand, unauthenticated 401, and brand-requires-company-first (400) cases; rebuilt `api` image, `alembic current` → head; live end-to-end curl flow against Compose (PUT/GET `/company/profile`, PUT `/brand/profile`) verified correct persisted data. Commit `e40f6f8` |
| GRX-COMPANY-002 | Company Settings | Company settings screen | Frontend form for company profile/brand voice | P1 | DONE | GRX-COMPANY-001 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/dashboard/company-settings/`, `apps/api/src/growixa_api/auth/{schemas,api}.py` (`/auth/me` now returns `permissions`), `apps/api/src/growixa_api/permissions/repositories.py` (`list_permission_codes_for_user`) | Settings persist and reload correctly through the UI | Frontend component test | — | 2026-07-27 | 2026-07-27 | Extended `/auth/me` to return the caller's permission codes so the frontend can render editable vs. read-only without guessing from a role name — Admin/Super Admin get an editable form, everyone else sees the same data disabled with a view-only note (per RBAC.md's `company.settings.edit` matrix). Saves company then brand profile in sequence (brand requires company to exist first). Sidebar gained a real SETTINGS section; topbar title is now route-driven. Found and fixed a real RTL bug while writing the component test: Vitest doesn't auto-wire `@testing-library/react`'s cleanup without `test.globals: true`, so DOM was leaking between tests in the same file — fixed via explicit `afterEach(cleanup)` in `vitest.setup.ts`. `ruff`/`mypy` clean, `pytest` 45 passed/2 skipped; frontend lint/format/typecheck/build clean, Vitest 5 passed (4 new), Playwright 3 passed (unaffected). Live-verified in a real browser against rebuilt Compose containers as both an Admin (editable, save persists, reload shows persisted values) and a Viewer (identical data, all fields disabled, no Save button). Commit `9989373` |
| GRX-FOUND-008 | Foundation | Dashboard shell | Authenticated layout, navigation, empty-state dashboard landing page | P1 | DONE | GRX-AUTH-002 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/{login,dashboard}/`, `apps/web/src/lib/auth.ts`, `apps/web/src/app/globals.css`, `apps/api/src/growixa_api/{app,config}.py` (CORS), `apps/api/src/growixa_api/auth/api.py` (`GET /auth/me`), `apps/web/tests/e2e/dashboard.spec.ts` | Logged-in user sees dashboard shell; logged-out user is redirected to login | Frontend e2e smoke test | — | 2026-07-27 | 2026-07-27 | Required two small, necessary backend additions discovered while scoping this frontend task (not in its original Files/Modules column): CORS middleware (`cors_allowed_origins` setting) so the browser can complete cross-origin credentialed requests from the web origin, and `GET /auth/me` (identifies the session via the access-token cookie, same shape as `/refresh`/`/logout-all` — added to the route-protection audit's allowlist) so a Server Component can determine auth state, since the access token is HttpOnly and unreadable by client JS. `ruff`/`mypy` clean; `pytest` 45 passed, 2 skipped; frontend `eslint`/`prettier --check`/`tsc --noEmit`/Vitest all clean. Playwright e2e (3 tests) passed against real Compose Postgres/Redis: logged-out `/dashboard` → redirect to `/login`; full login → dashboard shell (sidebar, top bar, user name, empty state) → logout → re-visiting `/dashboard` redirects to `/login` again, proving the session was actually cleared. **Real bug found and fixed during Compose verification** (not caught by the host-run Playwright suite, which never exercises the Dockerized `web` container): server-side fetches from inside the `web` container used `NEXT_PUBLIC_API_URL=http://localhost:8000`, but `localhost` inside that container is itself, not the `api` container — added a server-only `API_INTERNAL_URL=http://api:8000` (falls back to `NEXT_PUBLIC_API_URL` outside Docker) and set it in `compose.yaml`. Verified visually in a real browser against the rebuilt `web`+`api` containers: login screen renders the dark-navy glass-card design, dashboard shell renders sidebar/top bar/empty state, logout clears the session for real. Per `DESIGN_REFERENCES.md`'s scope caveat, only the Dashboard nav item is real (wired, active); no other nav item has a route yet. Commit `886329a` |
| GRX-TEST-001 | Testing | Backend test foundation | Test runner config, DB test fixtures/factories, coverage baseline | P0 | DONE | GRX-FOUND-005 (DONE) | coding agent | `apps/api/tests/conftest.py`, `apps/api/pyproject.toml` (pytest-cov, `integration` marker) | `pytest` runs green on a trivial test in CI | CI run | — | 2026-07-25 | 2026-07-25 | `ruff`/`mypy` clean; `pytest` 14 passed with coverage reporting enabled (87% baseline on `growixa_api`); `pytest -m "not integration"` (7 tests) verified to pass with zero DB/Redis/RabbitMQ reachable, confirming the unit/integration split is real, not just labeled. No CI pipeline exists yet — that is `GRX-DEVOPS-001`, which depends on this task; `pytest` running green locally is this task's actual deliverable. Commit `de55382` |
| GRX-TEST-002 | Testing | Frontend test foundation | Test runner config, component test harness, one e2e smoke test | P0 | DONE | GRX-FOUND-004 (DONE) | coding agent | `apps/web/vitest.config.ts`, `apps/web/vitest.setup.ts`, `apps/web/playwright.config.ts`, `apps/web/src/app/page.test.tsx`, `apps/web/tests/e2e/smoke.spec.ts` | Test suite runs green on a trivial test in CI | CI run | — | 2026-07-27 | 2026-07-27 | Picked ahead of `GRX-FOUND-008` because that task's own "Required Tests" (frontend e2e smoke test) can't be produced without a test runner existing first — same reasoning as why `GRX-TEST-001` preceded most backend feature work; `npm run lint`/`format:check`/`typecheck` all pass; `npm run test` (Vitest + React Testing Library, jsdom) → 1 passed (renders `HomePage`'s heading); `npm run test:e2e` (Playwright, Chromium) → 1 passed (root route returns 200, heading visible) against a real `next build && next start`; rebuilt `web` image with the new devDependencies, confirmed `/` still 200 and unknown route still 404 in Compose. No CI pipeline exists yet — that is `GRX-DEVOPS-001`, which depends on this task; the test suite running green locally is this task's actual deliverable (mirrors `GRX-TEST-001`'s equivalent evidence). Commit `a804186` |
| GRX-DEVOPS-001 | DevOps | CI pipeline | Lint, format check, type check, backend tests, frontend tests, Alembic migration check on every PR | P0 | IN_REVIEW | GRX-TEST-001 (DONE), GRX-TEST-002 (DONE) | coding agent | `.github/workflows/ci.yml` | CI passes on a clean baseline branch | CI run | — | 2026-07-27 | — | Implements all 8 stages from `TEST_STRATEGY.md` §CI test stages, split across 3 jobs: `backend` (lint/format/typecheck/`alembic check`/`pytest`, against real GitHub Actions Postgres+Redis+RabbitMQ service containers — Redis is host-reachable in CI, unlike local dev per `GRX-FOUND-006`'s finding, so tests that skip locally actually execute here), `frontend` (lint/format/typecheck/Vitest/`next build`, parallel to `backend`), and `e2e` (`needs: [backend, frontend]` — only stands up the real Compose stack, i.e. `docker compose up postgres redis rabbitmq api` plus Playwright's own webServer for the frontend, once the cheaper checks already passed). Made `apps/web/tests/e2e/{global-setup,global-teardown}.ts` compose-binary-portable (`COMPOSE_BIN` env var, defaults to `podman` for local dev, CI sets `docker`) so the same e2e suite runs unmodified in both environments. Locally re-verified every command the workflow runs: `ruff check`/`ruff format --check`/`mypy` clean; `alembic upgrade head && alembic check` → "No new upgrade operations detected"; `pytest` → 45 passed, 2 skipped; frontend `eslint`/`prettier --check`/`tsc --noEmit`/`vitest run` all clean; `next build` → succeeds, correctly classifies `/` and `/login` static, `/dashboard` dynamic; `npm run test:e2e` → 3 passed with the new `COMPOSE_BIN` parameterization (defaulting to `podman`, unchanged local behavior). Workflow YAML syntax-validated via `python3 -c "import yaml; yaml.safe_load(...)"`. **Left at `IN_REVIEW`, not `DONE`: a real green run on GitHub Actions has not been observed** — this requires pushing to the remote, which is a permission-gated action this session will not take without the user's explicit go-ahead (see AGENT_HANDOFF.md). Move to `DONE` once the user pushes and a run is confirmed green. Commit `7ff54dc` |
| GRX-DOC-003 | Documentation | Sprint 1 documentation + handoff update | Update `PROJECT_STATUS.md`, `FEATURE_STATUS_MATRIX.md`, `CHANGELOG.md`, `AGENT_HANDOFF.md` after Sprint 1 completes | P0 | BACKLOG | All Sprint 1 tasks above | unassigned | `docs/00-project-control/` | All Sprint 1 acceptance criteria in [SPRINT_01_FOUNDATION.md](../14-sprints/SPRINT_01_FOUNDATION.md) verified and recorded | N/A (documentation) | — | — | — | — |
| GRX-CONTACT-001 | Contacts | Contacts schema + CRUD | `contacts`, `contact_custom_fields`, `contact_field_values` tables; create/edit/archive API with email dedup; `contacts.manage`/`contacts.view` permission seed | P0 | DONE | GRX-FOUND-005 (DONE), GRX-RBAC-001 (DONE), GRX-AUTH-001 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/`, migration `209d29349ccf`, `apps/api/tests/test_contacts.py` | Admin can create, edit, archive a contact; duplicate email updates instead of duplicating; Viewer-equivalent (`contacts.view` only) cannot write | Integration tests for CRUD + dedup + permission split | — | 2026-07-30 | 2026-07-30 | `POST /contacts` (create-or-update-by-email), `GET /contacts`, `GET /contacts/{id}`, `PATCH /contacts/{id}`, `PATCH /contacts/{id}/status`, plus `GET`/`POST /contacts/custom-fields`. Email is the sole dedup key — creating with an existing email updates that row instead of duplicating (verified live: same id returned, list stays at 1). Contact activity reuses `audit_logs` (`contact.created`/`updated`/`archived`) rather than a new table. `contacts.manage` → Super Admin/Admin/Marketing Manager; `contacts.view` → those three plus Analyst (Viewer gets neither, per RBAC.md's Slice 2 rationale). Found and fixed a real bug during live verification: `updated_at`'s server-side `onupdate` gets expired after `session.commit()`, and reading it synchronously afterward raised `MissingGreenlet`; fixed with an explicit `await session.refresh(contact)` post-commit in every mutating service function. Updated `test_auth_schema_seed.py`'s exact permission-set assertion to include the two new codes (a legitimate extension, not a regression). `ruff`/`mypy` clean; `pytest` 69 passed, 3 skipped (94% coverage, 12 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: created a contact with a custom field, re-created with the same email (dedup confirmed via matching id + list count), archived/reactivated (audit event confirmed), and confirmed Analyst gets 200 on GET/403 on POST while Viewer gets 403 on both. Commit `7ec6c93` |
| GRX-CONTACT-002 | Contacts | Tags & lists | `tags`, `contact_tags`, `contact_lists`, `contact_list_members` tables + API | P0 | DONE | GRX-CONTACT-001 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `788ff9dd33db`, `apps/api/tests/test_contacts_tags_and_lists.py` | Contacts can be tagged and added to a manually curated list | Integration tests | — | 2026-07-30 | 2026-07-30 | Added `tags`/`contact_tags` and `contact_lists`/`contact_list_members` tables. `GET`/`POST /contacts/tags`, `POST`/`DELETE /contacts/{id}/tags[/{tag_id}]` (attach/detach), `GET`/`POST /contacts/lists`, `GET /contacts/lists/{id}`, `POST`/`DELETE /contacts/lists/{id}/members[/{contact_id}]`. `ContactOut` now includes `tags: list[str]`; `ContactListOut` includes a live `member_count`. Attach/detach and list-membership changes emit `contact.tagged`/`contact.list_added` audit events (reusing `audit_logs`, per the Slice 2 design decision — no new activity table). No RBAC changes needed — `contacts.manage`/`contacts.view` already covered tags/lists per RBAC.md's Slice 2 description. `ruff`/`mypy` clean; `pytest` 76 passed, 3 skipped (94% coverage, 7 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: created a tag and attached/detached it from a contact (tags array updated both times), created a list and added/removed a member (member_count went 0→1→0), confirmed Analyst gets 200 on GET/403 on POST for both tags and lists while Viewer gets 403 on both. `GRX-CONTACT-003` (segments) is now the next `READY` task. Commit `1e69461` |
| GRX-CONTACT-003 | Contacts | Segments | `segments`, `segment_rules`, `segment_members` tables + API; AND-only rule evaluation; DYNAMIC live-evaluated, SAVED frozen snapshot | P0 | DONE | GRX-CONTACT-002 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `18cf808f2b87`, `apps/api/tests/test_contacts_segments.py` | A segment's membership can be computed from its rules (DYNAMIC) or frozen at save time (SAVED) | Integration tests for both segment types | — | 2026-07-30 | 2026-07-31 | Added `segments`/`segment_rules`/`segment_members` tables and a rule evaluator supporting `status`/`email`/`source` (equals; email also contains), `tag` (equals), `created_at` (before/after), and `custom_field:<key>` (equals/contains) — all AND-combined only, per the Slice 2 scope decision. `consent_status` was named as an example field in early planning but isn't supported yet since `consent_records` doesn't exist until GRX-CONTACT-005; using it now correctly 400s as an unsupported field rather than silently matching nothing. `POST /contacts/segments` validates every rule (unknown field/operator/custom-field-key/unparseable date all 400) before creating anything. `DYNAMIC` segments compute membership live on every read; `SAVED` segments evaluate once at creation and freeze into `segment_members` — verified live that tagging a new contact after creation changes a DYNAMIC segment's count but not a SAVED one's. `ruff`/`mypy` clean; `pytest` 85 passed, 3 skipped (93% coverage, 9 new tests); `alembic check` → no drift (had to add `index=True` to `SegmentRule.segment_id` in the ORM model to match the migration's explicit index, caught by `alembic check` itself). Live-verified against rebuilt Compose containers: created a DYNAMIC and a SAVED segment against the same rule, tagged a second contact afterward, confirmed DYNAMIC's count went 1→2 while SAVED stayed at 1; confirmed Analyst gets 200 on GET/403 on POST, Viewer gets 403. This was the last backend-only Slice 2 task — `GRX-CONTACT-004` (CSV import) is next `READY`. Commit `a725e0e` |
| GRX-CONTACT-004 | Contacts | CSV contact import | `contact_imports`, `contact_import_rows` tables + API; upload, column mapping, validation, per-row status, import history | P0 | DONE | GRX-CONTACT-002 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `b11cffc2cbcf`, `apps/api/tests/test_contacts_import.py` | A CSV file can be imported, producing a history entry with imported/skipped/error counts; duplicate emails update existing contacts | Integration tests incl. malformed rows and duplicate-email handling | — | 2026-07-31 | 2026-07-31 | Added `contact_imports`/`contact_import_rows` tables. `POST /contacts/imports` accepts multipart upload (`file` + a `column_mapping` JSON string mapping CSV headers to `email`/`first_name`/`last_name`/`phone`/`source`/`custom_field:<key>`); processes synchronously and returns imported/updated/skipped/error counts. Row outcomes: blank rows → `SKIPPED`, missing email → `ERROR`, unknown custom-field key → 400 at mapping-validation time (before any row is processed) or `ERROR` per-row if the key check races; existing-email rows reuse `create_or_update_contact`'s dedup logic and are marked `UPDATED`, new emails `IMPORTED`. `GET /contacts/imports`, `GET /contacts/imports/{id}`, `GET /contacts/imports/{id}/rows` expose history and per-row detail. No new permissions — reuses `contacts.manage`/`contacts.view`. Added `python-multipart` dependency for FastAPI's `UploadFile`/`Form` handling, and extended ruff's `flake8-bugbear` immutable-calls allowlist with `fastapi.File`/`fastapi.Form`. `ruff`/`mypy` clean; `pytest` 93 passed, 3 skipped (8 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: uploaded a 3-row CSV (one new email, one pre-existing email, one blank email) mapped to `email`/`first_name`/`custom_field:plan` — got back `imported_count=1`, `updated_count=1`, `error_count=1`, confirmed per-row detail matched, confirmed the new contact carried its custom field and the existing contact's first name and custom field were both updated, confirmed a mapping without an `email` target 400s. This closes out Sprint 2's backend CSV import slice — `GRX-CONTACT-005` (consent & suppression) is next `READY`. Commit `52fb417` |
| GRX-CONTACT-005 | Contacts | Consent & suppression | `consent_records` (insert-only), `suppression_entries` (upsert-on-email) tables + API | P0 | DONE | GRX-CONTACT-001 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `97642610fb46`, `apps/api/tests/test_contacts_consent_and_suppression.py` | Consent per channel can be recorded and its history viewed; an email can be suppressed and re-suppression updates in place | Integration tests | — | 2026-07-31 | 2026-07-31 | Added `consent_records`/`suppression_entries` tables exactly per DATA_MODEL.md's field-level spec. `POST`/`GET /contacts/{id}/consent` record and list an insert-only consent history (channel `EMAIL`/`SMS`, status `GRANTED`/`WITHDRAWN`/`UNKNOWN`), newest-first — current status per channel is derived as "most recent row," never stored as separate mutable state. `POST /contacts/suppression` upserts on the unique `email` index: a new email creates a row, an already-suppressed email updates `reason`/`suppressed_at` in place (same id, no duplicate) — verified live both ways. `contact_id` is optional on suppression (an address can be suppressed with no matching contact, e.g. a hard bounce) but is validated to exist when given. Per the sprint's explicit acceptance criterion ("visibly flagged as suppressed wherever contacts are shown"), `ContactOut` gained `is_suppressed: bool`, computed from the suppression table at snapshot time — this widened `ContactSnapshot` to a 4-tuple across every contacts service function and `_to_out` call site in `api.py`. No new permissions — reuses `contacts.manage`/`contacts.view`, per RBAC.md's existing scope note ("record consent; suppress addresses" under `contacts.manage`). `suppressed_at` deliberately has no `onupdate` trigger — the upsert path sets it explicitly in Python, avoiding this session's known `MissingGreenlet` pattern entirely rather than needing a `session.refresh()`. `ruff`/`mypy` clean; `pytest` 101 passed, 3 skipped (8 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: recorded GRANTED then WITHDRAWN consent for a contact (history returned newest-first, 2 rows); suppressed a contact's email (contact's `is_suppressed` flipped true), re-suppressed with a different reason (same suppression id, reason updated, still exactly one list entry); suppressed an email with no contact at all (contact_id null); confirmed an unknown `contact_id` 404s. This closes out Sprint 2's entire backend slice (contacts, tags, lists, segments, CSV import, consent/suppression) — `GRX-CONTACT-006` (contacts frontend) is next `READY`. Commit `30b5fc2` |
| GRX-CONTACT-006 | Contacts | Contacts frontend | List/detail/create/edit UI for contacts, gated on `contacts.manage`/`contacts.view` | P1 | READY | GRX-CONTACT-001 (DONE), GRX-FOUND-004 (DONE) | unassigned | `apps/web/src/app/dashboard/contacts/` | Admin can manage contacts through the UI; a view-only user sees the same data with no write controls | Frontend component tests | — | — | — | — |
| GRX-CONTACT-007 | Contacts | Tags/lists/segments frontend | UI for tag/list assignment and segment builder (rule editor) | P1 | BACKLOG | GRX-CONTACT-002, GRX-CONTACT-003, GRX-CONTACT-006 | unassigned | `apps/web/src/app/dashboard/contacts/` (extended) | Admin can tag contacts, manage lists, and build/save a segment through the UI | Frontend component tests | — | — | — | — |
| GRX-CONTACT-008 | Contacts | CSV import frontend | Upload UI, column-mapping step, import-history view | P1 | BACKLOG | GRX-CONTACT-004, GRX-CONTACT-006 | unassigned | `apps/web/src/app/dashboard/contacts/` (extended) | Admin can upload a CSV, map columns, and see import results in the UI | Frontend component tests | — | — | — | — |
| GRX-CONTACT-009 | Contacts | Consent/suppression frontend | UI to view consent history and manage the suppression list | P1 | BACKLOG | GRX-CONTACT-005, GRX-CONTACT-006 | unassigned | `apps/web/src/app/dashboard/contacts/` (extended) | Admin can view a contact's consent history and add/remove a suppression entry through the UI | Frontend component tests | — | — | — | — |

## Explicitly not tracked here (do not add without a new decision)

Email campaigns, social integrations, AI generation, SEO/AEO/GEO/crawler work, WordPress/GitHub
integration, billing — all out of Sprint 1 and Slice 1 scope. See
[SPRINT_01_FOUNDATION.md §Exclusions](../14-sprints/SPRINT_01_FOUNDATION.md#explicitly-excluded-from-sprint-1).
