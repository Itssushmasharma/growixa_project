# Master Task Tracker

- Document ID: DOC-TASK-TRACKER
- Status: ACTIVE
- Version: 2.10
- Last updated: 2026-08-07
- Owner: Coding agent
- Related documents: [SPRINT_01_FOUNDATION](../14-sprints/SPRINT_01_FOUNDATION.md), [SPRINT_02_CONTACTS](../14-sprints/SPRINT_02_CONTACTS.md), [SPRINT_03_EMAIL_CAMPAIGN](../14-sprints/SPRINT_03_EMAIL_CAMPAIGN.md), [SPRINT_05_MULTI_TENANT_PLATFORM](../14-sprints/SPRINT_05_MULTI_TENANT_PLATFORM.md), [DEVELOPMENT_READINESS](DEVELOPMENT_READINESS.md), [FEATURE_STATUS_MATRIX](FEATURE_STATUS_MATRIX.md)

Statuses: `BACKLOG`, `READY`, `IN_PROGRESS`, `BLOCKED`, `IN_REVIEW`, `TESTING`, `DONE`,
`DEFERRED`, `CANCELLED`. Priorities: `P0`, `P1`, `P2`, `P3`.

No task below `READY` may be started (per [AGENT_EXECUTION_RULES.md](../12-development/AGENT_EXECUTION_RULES.md)).
A task becomes `READY` only after its dependencies are `DONE` and the
[Development Readiness Gate](DEVELOPMENT_READINESS.md) passes for its slice.

| Task ID | Epic | Task | Description | Priority | Status | Dependencies | Assigned Agent | Files/Modules | Acceptance Criteria | Required Tests | Blocker | Started At | Completed At | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GRX-DOC-001 | Documentation | Establish Growixa documentation foundation | Phase 0/1 docs: product vision, PRD, MVP scope, roadmap, future-scope mapping, decisions, assumptions, open questions, DoD, agent execution rules | P0 | DONE | — | coding agent | `docs/00-project-control/`, `docs/01-product/`, `docs/12-development/` | Docs exist, are internally consistent, and readiness items they cover show PASS | N/A (documentation) | — | 2026-07-22 | 2026-07-22 | Commit `50b93cf` |
| GRX-DOC-002 | Documentation | Architecture, data, and security baseline docs | SYSTEM_ARCHITECTURE, MODULE_BOUNDARIES, BACKGROUND_JOB_ARCHITECTURE, DATA_MODEL, ERD, DATABASE_SCHEMA, SECURITY_ARCHITECTURE, AUTHENTICATION, RBAC, THREAT_MODEL | P0 | DONE | GRX-DOC-001 | coding agent | `docs/04-architecture/`, `docs/05-data/`, `docs/08-security/` | Development Readiness Gate items for these areas show PASS | N/A (documentation) | — | 2026-07-22 | 2026-07-22 | This commit |
| GRX-FOUND-001 | Foundation | Repository and development tooling | Backend/frontend project scaffolding, linting, formatting, type-checking config, pre-commit hooks, env var examples | P0 | DONE | GRX-DOC-002 | coding agent | `apps/api/` (pyproject.toml, .venv tooling, .env.example), `apps/web/` (package.json, eslint/prettier/tsconfig, .env.example), `.pre-commit-config.yaml`, `.gitignore` | Lint/format/type-check run clean on empty scaffolding; `.env.example` present | Config validation only | — | 2026-07-23 | 2026-07-23 | `ruff check .`→0 errors; `ruff format --check .`→pass; `mypy .`→0 issues; `npm run lint`→pass (0 errors); `npm run format:check`→pass; `npm run typecheck`→pass; `npm audit`→0 vulnerabilities; `pre-commit run --all-files`→all hooks pass. Commit `42f8b37` |
| GRX-FOUND-002 | Foundation | Docker Compose local environment | Compose file for Postgres, Redis, RabbitMQ, backend, frontend | P0 | DONE | GRX-FOUND-001 (DONE) | coding agent | `compose.yaml`, `apps/api/Dockerfile`, `apps/api/src/growixa_api/{main,config}.py`, `apps/web/Dockerfile`, `apps/web/next.config.mjs`, `apps/web/src/app/`, root `.env.example` | `docker compose up` starts all services; backend/frontend reach DB/Redis/RabbitMQ | Manual + smoke test | — | 2026-07-23 | 2026-07-23 | `podman compose up -d` → all 5 containers (`postgres`, `redis`, `rabbitmq`, `api`, `web`) report `healthy`; `GET /health` → `{"status":"ok","checks":{"postgres":"ok","redis":"ok","rabbitmq":"ok"}}`; `GET /` on web → 200. Commit `76354d2` |
| GRX-FOUND-003 | Foundation | FastAPI application foundation | App factory, config loading, health endpoint, OpenAPI docs enabled | P0 | DONE | GRX-FOUND-002 (DONE) | coding agent | `apps/api/src/growixa_api/{app,health,main}.py`, `apps/api/tests/test_health.py` | `GET /health` returns 200 locally and in Docker Compose | Unit test for health endpoint | — | 2026-07-23 | 2026-07-23 | `ruff check` 0 errors, `ruff format --check` pass, `mypy` 0 issues, `pytest` 2 passed; `podman compose up -d` → all 5 healthy; `GET /health` → 200 `{"status":"ok",...}`; `GET /docs`, `/openapi.json` → 200. Commit `c69eb10` |
| GRX-FOUND-004 | Foundation | Next.js application foundation | App shell, routing baseline, API client setup, env config | P0 | DONE | GRX-FOUND-002 (DONE) | coding agent | `apps/web/src/app/{page,not-found}.tsx`, `apps/web/src/lib/{env,api-client}.ts` | App builds and serves a placeholder page locally and in Docker Compose | Build passes; smoke test loads root route | — | 2026-07-24 | 2026-07-24 | `npm run lint`/`format:check`/`typecheck`/`build` all pass; local `next start` → `/` 200 (renders "Growixa"), unknown route → 404; `podman compose up -d --build web` → all 5 healthy, `GET /` → 200, unknown route → 404. Commit `8269436` |
| GRX-FOUND-005 | Foundation | PostgreSQL connectivity + Alembic foundation | SQLAlchemy engine/session setup, Alembic init, first empty migration runs | P0 | DONE | GRX-FOUND-003 (DONE) | coding agent | `apps/api/src/growixa_api/db.py`, `apps/api/alembic.ini`, `apps/api/migrations/`, `apps/api/tests/test_migrations.py` | `alembic upgrade head` succeeds against Compose Postgres | Migration smoke test | — | 2026-07-24 | 2026-07-24 | `ruff check` 0 errors, `ruff format --check` pass, `mypy` 0 issues (11 files), `pytest` 3 passed incl. real upgrade/downgrade round-trip; `alembic upgrade head`/`alembic current` succeed both from host (→ localhost:5433) and via `podman compose exec api alembic upgrade head` against Compose Postgres. Commit `0cff500` |
| GRX-FOUND-006 | Foundation | Redis connectivity | Redis client wiring, used later for rate limiting/locks/idempotency | P0 | DONE | GRX-FOUND-003 (DONE) | coding agent | `apps/api/src/growixa_api/redis.py` (new), `apps/api/src/growixa_api/health.py` (reuses pooled client), `apps/api/tests/test_redis.py` | Backend can set/get a key against Compose Redis | Connectivity smoke test | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 38 passed, 1 skipped (95% coverage) — the new Redis smoke test skips under host-run pytest because Compose's `redis` service deliberately has no host port mapping (documented in `LOCAL_DEVELOPMENT.md`; only Postgres/RabbitMQ are host-reachable), a documented environment fact, not a code defect; rebuilt `api` image, `GET /health` → `{"status":"ok",...}` using the new pooled client (replacing health.py's old per-request throwaway client), and a live set/get/delete against real Compose Redis executed inside the `api` container via the shared client confirmed round-trip correctness. Commit `32aacdf` |
| GRX-FOUND-007 | Foundation | RabbitMQ connectivity and health check | Producer/consumer connection setup, `grx.system.healthcheck` queue and worker skeleton per [BACKGROUND_JOB_ARCHITECTURE.md](../04-architecture/BACKGROUND_JOB_ARCHITECTURE.md) | P0 | DONE | GRX-FOUND-003 (DONE) | coding agent | `apps/api/src/growixa_api/jobs/` (new), `apps/worker/` (new) | Worker consumes a health-check message published by the API | Connectivity smoke test | — | 2026-07-29 | 2026-07-29 | Added the shared `JobEnvelope` schema (`jobs/schemas.py`) and `publish_job()` producer (`jobs/producer.py`) to `apps/api`, plus an `admin.access`-gated `POST /system/jobs/healthcheck` to trigger one for testability (no business-job endpoint exists in Sprint 1 otherwise). New standalone `apps/worker/` app (own pyproject.toml/Dockerfile, mirrors `apps/api`'s tooling) connects to RabbitMQ, declares the `grx.system.healthcheck` queue, and logs each job it processes — an independently deployable process per SYSTEM_ARCHITECTURE.md, not a thread inside the API. Wired into `compose.yaml` as a new `worker` service and into `ci.yml` as a new parallel job (its own test suite is pure-unit, no service containers needed). `ruff`/`mypy` clean on both apps; `apps/api` `pytest` 58 passed, 3 skipped (95% coverage) — the new RabbitMQ producer round-trip test skips locally for the same host-unreachable-broker reason as `GRX-FOUND-006`'s Redis test, but runs for real in CI; `apps/worker` `pytest` 2 passed. Live-verified against rebuilt Compose containers: `docker compose logs worker` showed the worker listening on the queue; triggered a real healthcheck job as a smoke Admin via curl, confirmed the worker's log line showed the exact same `job_id`; confirmed a Viewer gets 403; `/health`'s existing `rabbitmq` check still reports `ok`. Also corrected a stale placeholder in `LOCAL_DEVELOPMENT.md` (previously said the worker would build from `apps/api/`). Commit `a0a1eea` |
| GRX-AUDIT-001 | Audit | Audit log module | `audit_logs` table/migration, insert-only repository/service, structured-log redaction of sensitive fields | P0 | DONE | GRX-FOUND-005 (DONE), GRX-AUTH-001 (DONE) | coding agent | `apps/api/src/growixa_api/audit/{models,repositories,services}.py`, migration `6575d09949f9`, `apps/api/tests/{test_audit_log,test_audit_insert_only}.py` | Audit events can be written and listed; no UPDATE/DELETE path exists | Unit tests for write/list; negative test for missing update/delete routes | — | 2026-07-25 | 2026-07-25 | `ruff`/`mypy` clean; `pytest` 14 passed incl. write/list round-trip, system-actor (NULL) event, sensitive-metadata redaction, and a negative test proving no update/delete function exists in `audit.repositories`/`audit.services`; `podman compose exec api alembic current` → head; `/health` unaffected. Commit `2b1dd7e` |
| GRX-AUDIT-002 | Audit | Audit log viewing (API + frontend) | `GET /audit` endpoint gated on `audit.view`, plus a frontend page to browse it | P1 | DONE | GRX-AUDIT-001 (DONE), GRX-RBAC-001 (DONE) | coding agent | `apps/api/src/growixa_api/audit/{api,schemas}.py` (new), `apps/web/src/app/dashboard/audit/` (new), `sidebar.tsx`, `page-title.tsx` | A user with `audit.view` can list/filter audit events through the UI; a user without it gets 403/no nav item | Integration tests + frontend component test | — | 2026-07-31 | 2026-07-31 | Filed 2026-07-31 during `GRX-DOC-003`'s feature-status audit: `GRX-AUDIT-001` built write-only (repository/service, no `api.py`), so Sprint 1's stated acceptance criterion ("audit logs... are visible to users with `audit.view`") was never actually met. Added `GET /audit` (optional `entity_type`/`actor_user_id` query filters, reusing `list_events` from `GRX-AUDIT-001`), gated on `audit.view` via the existing `require_permission` dependency — passes the route-protection audit test with no allowlist changes needed. **Real bug found and fixed**: `AuditLogOut.ip_address` was typed `str | None`, but Postgres `INET` comes back from asyncpg as an `ipaddress.IPv4Address` object, not a `str` — every row with a real IP 500'd on serialization until a `field_validator` stringified it; caught by `pytest`, not by manual testing, since the local dev DB already had a row with a real IP from earlier session traffic. Built `AuditPage` (`/dashboard/audit`): a read-only list (action, resolved actor email, timestamp, non-empty metadata inline, entity_type badge) with a client-side entity_type filter dropdown built from the loaded events (no re-fetch per filter change, since the backend already caps at 100 rows). Actor emails resolved via `GET /users` (gated `users.manage`, not `audit.view` — safe today since both permissions have identical Super Admin/Admin-only grants, same reasoning as `roles/api.py`'s existing comment). Added "Audit Log" to the sidebar's SETTINGS section (gated `audit.view`) and a page-title entry. `ruff`/`mypy` clean (confirmed the handful of pre-existing mypy errors elsewhere are unrelated via `git stash` diff); `pytest` 107 passed, 3 skipped (3 new: list, entity_type filter, 403 for a non-`audit.view` role); `alembic check` → no drift (no schema changes, `GRX-AUDIT-001`'s table already fit). `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest` 54 passed (4 new). Live-verified against rebuilt Compose containers: logged in as Super Admin, saw real `user.login`/`user.login_failed` events with resolved actor emails; created a contact and confirmed a new `contact.created` event appeared newest-first; filtered by entity_type to `contact` and confirmed only that row remained; confirmed a fresh Analyst (`audit.view`-less) sees no "Audit Log" nav item and gets an access-denied message on direct navigation. **Also recreated the `admin@growixa.local` smoke-test account**, which running the full `pytest` suite wiped again via `test_migrations.py`'s known table-drop side effect (documented in this file's `GRX-CONTACT-001`/`002` entries) — not a regression from this task. This closes `GRX-AUDIT-002` — no `BACKLOG` or `READY` tasks remain in the tracker. Commit `347a801` |
| GRX-AUTH-001 | Auth | Users, roles, permissions schema + seed | `users`, `roles`, `permissions`, `role_permissions`, `user_roles` tables/migrations, Sprint 1 seed data per [RBAC.md](../08-security/RBAC.md) | P0 | DONE | GRX-FOUND-005 (DONE) | coding agent | `apps/api/src/growixa_api/{roles,permissions,users}/models.py`, migration `d330e8b64b48`, `apps/api/tests/test_auth_schema_seed.py` | Migration seeds 6 roles and Sprint 1 permission codes | Migration test | — | 2026-07-24 | 2026-07-24 | `ruff check`/`format --check`/`mypy` clean; `pytest` 4 passed incl. seed-data assertion (6 roles, 6 permission codes, exact 16-row role→permission matrix per RBAC.md) and full upgrade/downgrade round-trip; verified directly via `psql` and via `podman compose exec api alembic current` → head. Commit `daf9bc7` |
| GRX-AUTH-002 | Auth | Password hashing + login/logout | Argon2id hashing, login endpoint, access/refresh cookie issuance, logout | P0 | DONE | GRX-AUTH-001 (DONE), GRX-AUDIT-001 (DONE) | coding agent | `apps/api/src/growixa_api/auth/{models,security,tokens,repositories,services,schemas,api}.py`, migration `ea25a5343142`, `apps/api/src/growixa_api/users/repositories.py`, `apps/api/tests/test_auth_login.py` | Valid login issues cookies and `user.login` audit event; invalid login returns generic error and `user.login_failed` event | Unit + integration tests incl. failure path | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 23 passed (94% coverage — corrected from a coverage.py+greenlet tracing bug found and fixed in this task) incl. valid login (cookies, no tokens in body, audit event), identical generic error for wrong-password/unknown-email, disabled-account rejection, logout (revokes token, clears cookies, audit event); rebuilt `api` image, `alembic current` → head; live curl flow against Compose confirmed cookie attributes and behavior. Commit `b7cf4d8` |
| GRX-AUTH-003 | Auth | Refresh-token rotation + session revocation | Rotating refresh tokens, reuse detection, logout-all-sessions, account disable revokes sessions | P0 | DONE | GRX-AUTH-002 (DONE) | coding agent | `apps/api/src/growixa_api/auth/{repositories,services,api}.py` (extended), `apps/api/tests/test_auth_refresh.py` | Refresh rotates on use; reused old token revokes chain; disable revokes sessions | Integration tests for rotation, reuse detection, disable | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 28 passed (94% coverage) incl. rotation (new cookies, old token revoked), reuse detection (revokes entire chain, `session.revoked` audit event), `logout-all` (revokes every session across simulated devices), disable-revokes-sessions (via the reusable `revoke_all_active_sessions()`), and expired-token rejection; rebuilt `api` image; live curl rotation+reuse flow against Compose confirmed both tokens revoked and audit event recorded via direct DB query. Commit `27a22af` |
| GRX-AUTH-004 | Auth | Login rate limiting | Redis-backed rate limit on login + password-reset-request endpoints (T1/T12 in [THREAT_MODEL.md](../08-security/THREAT_MODEL.md)) | P0 | DONE | GRX-AUTH-002 (DONE), GRX-FOUND-006 (DONE) | coding agent | `apps/api/src/growixa_api/auth/rate_limit.py` (new), `apps/api/src/growixa_api/auth/api.py` (login + password-reset-request routes extended), `apps/api/src/growixa_api/config.py` (`rate_limit_max_attempts`/`rate_limit_window_seconds`), `apps/api/tests/test_auth_rate_limit.py` | Exceeding threshold returns 429; legitimate traffic unaffected | Unit test for limiter logic | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 43 passed, 2 skipped (95% coverage) incl. 5 pure unit tests against a fake Redis stand-in (allows up to max, raises past max, isolates by identifier, isolates by bucket, fails open on `RedisError`) plus an integration test that skips under host-run pytest for the same documented Redis-unreachable-from-host reason as `GRX-FOUND-006`; rebuilt `api` image and verified live against real Compose Redis: 6 wrong-password attempts against one email+IP returned `401×5` then `429`, a correct password against that same already-limited identifier also correctly returned `429` (rate limiting is identity-keyed, not correctness-keyed — this is the intended brute-force defense, not a bug), a different email+IP logged in normally (`200`, proving legitimate traffic elsewhere is unaffected), and the same 5-then-429 pattern was confirmed independently on `/auth/password-reset/request`; verified via `redis-cli KEYS "grx:ratelimit:*"` that keys use the exact `grx:ratelimit:{bucket}:{identifier}` naming `LOCAL_DEVELOPMENT.md` documents, then cleaned up. Design note: the limiter fails open (allows the request) on any `RedisError` so a Redis outage degrades security rather than availability — an explicit, flagged engineering call, not a gap; see CHANGELOG. Commit `5c26200` |
| GRX-AUTH-005 | Auth | Password reset flow | Request + completion endpoints, hashed single-use tokens | P0 | DONE | GRX-AUTH-002 (DONE), GRX-AUDIT-001 (DONE) | coding agent | `apps/api/src/growixa_api/auth/{models,repositories,services,schemas,api}.py` (extended), `apps/api/src/growixa_api/config.py` (`password_reset_ttl_minutes`), migration `bb25de08ba84`, `apps/api/tests/test_auth_password_reset.py` | Reset completes, invalidates token, revokes existing sessions, emits audit events | Integration tests | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 38 passed (95% coverage) incl. full request→complete round-trip (old password rejected, new password works, active sessions revoked, `user.password_reset_requested`/`user.password_reset_completed`/`session.revoked` all emitted), identical generic response for known vs. unknown email, and invalid/reused-token rejection; also verified the response's raw `token` field is `null` for both known and unknown email once `ENVIRONMENT != local` (a monkeypatched-settings test), confirming THREAT_MODEL.md T11 holds outside local dev; rebuilt `api` image, `alembic current` → head; live curl request→complete flow against Compose confirmed via direct DB query (audit events, session revocation, token `used_at`). Interim scope note: the raw token is only ever returned to the caller when `settings.environment == "local"` (same pattern as GRX-USER-001's invitation token) — no email-delivery channel exists yet in Sprint 1. Commit `730519b` |
| GRX-USER-001 | User Management | Internal user invitation + acceptance | Admin invites by email + role; invitee accepts via token | P0 | DONE | GRX-AUTH-001 (DONE), GRX-AUDIT-001 (DONE) | coding agent | `apps/api/src/growixa_api/users/{models,repositories,services,schemas,api}.py`, `apps/api/src/growixa_api/roles/repositories.py`, migration `f356da0136c3`, `apps/api/tests/test_users_invitations.py` | Invitation created, accepted, user provisioned with assigned role, `invitation.accepted` emitted | Integration tests | — | 2026-07-27 | 2026-07-27 | `ruff`/`mypy` clean; `pytest` 34 passed (94% coverage) incl. full invite→accept round-trip (role assigned, `invitation.accepted` audited), non-admin 403, unknown-role 400, duplicate-email 409, invalid/already-accepted token 400; rebuilt `api` image, `alembic current` → head; live curl invite+accept flow against Compose confirmed role assignment and audit event via direct DB query. Raw invitation token is returned in the create-invitation API response (no email-delivery channel exists in Sprint 1 to send it — flagged, not silently done) — see CHANGELOG. Commit `87d2110` |
| GRX-USER-002 | User Management | User management screens (list, invite, disable, role assignment) | Admin-facing user list/detail UI | P1 | DONE | GRX-USER-001 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/dashboard/team/`, `apps/api/src/growixa_api/{users,roles}/` (extended) | Admin can invite, view, disable a user and change role through the UI | Frontend component tests + one e2e happy path | — | 2026-07-27 | 2026-07-29 | Added `GET /roles` (gated on `users.manage`, not `roles.manage` — it only backs the role-picker dropdown), `GET /users` (list with roles/status), `PATCH /users/{id}/status` and `/role`. Self-disable is blocked (`SelfActionNotAllowedError`) to avoid an unrecoverable lockout with no `seed_first_admin` CLI yet; disabling revokes all active sessions; role changes emit a `role.changed` audit event with old/new roles. New Team page (`/dashboard/team`) lists members, invites, changes roles, and disables/enables, gated on `users.manage`; sidebar's Team link only shows for users with that permission. `ruff`/`mypy` clean, `pytest` 55 passed (95% coverage, 10 new); frontend lint/format/typecheck/build clean, Vitest 14 passed (5 new), Playwright 4 passed (1 new e2e: invite → accept → login happy path). Live-verified in a real browser against rebuilt Compose containers: invited a user, changed their role, disabled them (session revoked, login now 401), confirmed self-disable is blocked in the UI. This closes out the last tracked Sprint 1 task. Commit `22ba450` |
| GRX-RBAC-001 | RBAC | Centralized permission-check dependency | `require_permission()` FastAPI dependency wired to every non-public route | P0 | DONE | GRX-AUTH-001 (DONE) | coding agent | `apps/api/src/growixa_api/permissions/{repositories,dependencies}.py`, `apps/api/tests/test_require_permission.py`, `apps/api/tests/test_protected_routes_audit.py` | A user lacking a permission gets 403 on a protected route in a test | Unit + integration tests, incl. negative case | — | 2026-07-24 | 2026-07-24 | `ruff`/`mypy` clean; `pytest` 9 passed (allowed/403/401-missing/401-invalid cases using the real GRX-AUTH-001-seeded Viewer role, plus a route-protection audit over `create_app()`); rebuilt `api` image, `/health` unaffected. Commit `7e77444` |
| GRX-COMPANY-001 | Company Settings | Company profile + brand settings | `company_profile`/`brand_profiles` tables, edit/view endpoints, RBAC-gated | P0 | DONE | GRX-FOUND-005 (DONE), GRX-RBAC-001 (DONE) | coding agent | `apps/api/src/growixa_api/{company,brand}/{models,schemas,repositories,services,api}.py`, migration `1abf62872712`, `apps/api/tests/test_company_settings.py` | Admin can save company settings; Viewer can view but not edit | Integration tests for edit/view permission split | — | 2026-07-25 | 2026-07-25 | `ruff`/`mypy` clean; `pytest` 19 passed (86% coverage) incl. admin edit+view, viewer view-only (403 on edit) for both company and brand, unauthenticated 401, and brand-requires-company-first (400) cases; rebuilt `api` image, `alembic current` → head; live end-to-end curl flow against Compose (PUT/GET `/company/profile`, PUT `/brand/profile`) verified correct persisted data. Commit `e40f6f8` |
| GRX-COMPANY-002 | Company Settings | Company settings screen | Frontend form for company profile/brand voice | P1 | DONE | GRX-COMPANY-001 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/dashboard/company-settings/`, `apps/api/src/growixa_api/auth/{schemas,api}.py` (`/auth/me` now returns `permissions`), `apps/api/src/growixa_api/permissions/repositories.py` (`list_permission_codes_for_user`) | Settings persist and reload correctly through the UI | Frontend component test | — | 2026-07-27 | 2026-07-27 | Extended `/auth/me` to return the caller's permission codes so the frontend can render editable vs. read-only without guessing from a role name — Admin/Super Admin get an editable form, everyone else sees the same data disabled with a view-only note (per RBAC.md's `company.settings.edit` matrix). Saves company then brand profile in sequence (brand requires company to exist first). Sidebar gained a real SETTINGS section; topbar title is now route-driven. Found and fixed a real RTL bug while writing the component test: Vitest doesn't auto-wire `@testing-library/react`'s cleanup without `test.globals: true`, so DOM was leaking between tests in the same file — fixed via explicit `afterEach(cleanup)` in `vitest.setup.ts`. `ruff`/`mypy` clean, `pytest` 45 passed/2 skipped; frontend lint/format/typecheck/build clean, Vitest 5 passed (4 new), Playwright 3 passed (unaffected). Live-verified in a real browser against rebuilt Compose containers as both an Admin (editable, save persists, reload shows persisted values) and a Viewer (identical data, all fields disabled, no Save button). Commit `9989373` |
| GRX-FOUND-008 | Foundation | Dashboard shell | Authenticated layout, navigation, empty-state dashboard landing page | P1 | DONE | GRX-AUTH-002 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/{login,dashboard}/`, `apps/web/src/lib/auth.ts`, `apps/web/src/app/globals.css`, `apps/api/src/growixa_api/{app,config}.py` (CORS), `apps/api/src/growixa_api/auth/api.py` (`GET /auth/me`), `apps/web/tests/e2e/dashboard.spec.ts` | Logged-in user sees dashboard shell; logged-out user is redirected to login | Frontend e2e smoke test | — | 2026-07-27 | 2026-07-27 | Required two small, necessary backend additions discovered while scoping this frontend task (not in its original Files/Modules column): CORS middleware (`cors_allowed_origins` setting) so the browser can complete cross-origin credentialed requests from the web origin, and `GET /auth/me` (identifies the session via the access-token cookie, same shape as `/refresh`/`/logout-all` — added to the route-protection audit's allowlist) so a Server Component can determine auth state, since the access token is HttpOnly and unreadable by client JS. `ruff`/`mypy` clean; `pytest` 45 passed, 2 skipped; frontend `eslint`/`prettier --check`/`tsc --noEmit`/Vitest all clean. Playwright e2e (3 tests) passed against real Compose Postgres/Redis: logged-out `/dashboard` → redirect to `/login`; full login → dashboard shell (sidebar, top bar, user name, empty state) → logout → re-visiting `/dashboard` redirects to `/login` again, proving the session was actually cleared. **Real bug found and fixed during Compose verification** (not caught by the host-run Playwright suite, which never exercises the Dockerized `web` container): server-side fetches from inside the `web` container used `NEXT_PUBLIC_API_URL=http://localhost:8000`, but `localhost` inside that container is itself, not the `api` container — added a server-only `API_INTERNAL_URL=http://api:8000` (falls back to `NEXT_PUBLIC_API_URL` outside Docker) and set it in `compose.yaml`. Verified visually in a real browser against the rebuilt `web`+`api` containers: login screen renders the dark-navy glass-card design, dashboard shell renders sidebar/top bar/empty state, logout clears the session for real. Per `DESIGN_REFERENCES.md`'s scope caveat, only the Dashboard nav item is real (wired, active); no other nav item has a route yet. Commit `886329a` |
| GRX-FOUND-009 | Foundation | Collapsible/responsive sidebar navigation | Hamburger toggle (desktop collapse + mobile overlay drawer), plus a per-section accordion for each nav heading | P2 | DONE | GRX-FOUND-008 (DONE) | coding agent | `apps/web/src/app/dashboard/{dashboard-shell,sidebar,layout}.tsx` (new/extended), `sidebar.module.css`, `topbar.module.css`, `vitest.setup.ts` | Clicking the hamburger collapses/expands the whole sidebar (desktop: reflow; mobile: overlay drawer with backdrop); clicking a section heading (OVERVIEW/AUDIENCE/SETTINGS) independently expands/collapses just that section's items | Frontend component tests | — | 2026-08-01 | 2026-08-01 | Direct user request (ad hoc, not from a prior sprint plan), asked mid-session after `GRX-AUDIT-002`, in two parts. **Part 1 — whole-sidebar hamburger toggle.** Introduced this app's first responsive breakpoint (768px) — none existed before. New `DashboardShell` client component (extracted from `layout.tsx`, which stays a server component doing the auth check) owns a single `sidebarOpen` boolean and renders the hamburger button (inline SVG, no icon library in this project); `Sidebar` gained `open`/`onClose` props and a conditionally-rendered backdrop `<button>`. One boolean drives both breakpoints via CSS alone: at desktop widths `open=false` collapses width to 0 (flex reflow, no per-item icons exist so this is a full hide, not an icon-only rail); at mobile widths (`@media (max-width: 768px)`) the sidebar is `position: fixed` and translates off-screen by default, with `.mobileOpen` sliding it to `translateX(0)` — no JS media-query branching needed for the toggle itself. A `useEffect` keyed on `usePathname()` closes the drawer on navigation whenever `window.matchMedia("(max-width: 768px)")` matches, so navigating on mobile doesn't leave the drawer covering the new page; desktop navigation is unaffected since nothing calls `onClose` on link click there. Added a `window.matchMedia` polyfill to `vitest.setup.ts` (jsdom has none) — a real gap this exposed, now available to any future test needing it. **Part 2 — per-section accordion**, requested immediately after as a follow-up referencing a third-party product screenshot (per-heading expand/collapse, e.g. "Market"/"Automation"): each `NAV_SECTIONS` entry (OVERVIEW/AUDIENCE/SETTINGS) is now its own independent accordion via a `Record<string, boolean>` keyed by section label, defaulting every section to expanded (non-breaking — matches prior always-visible behavior); the plain `<span>` section label became a `<button>` with `aria-expanded` and a rotating chevron SVG. Complementary to, not a replacement for, the whole-sidebar toggle — collapsing one section doesn't affect the others or the sidebar's own open/closed state. `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest` 54 passed (4 new: default-open desktop + toggle, default-closed mobile via a `matchMedia` spy, permission-based nav filtering still holds through the prop changes, and independent per-section collapse/expand); `npm run test:e2e` (Playwright, 4 tests, unaffected by either change) still passes against a real `next build`. Live-verified against the rebuilt `web` container at both `desktop` and `mobile` (375×812) presets: desktop hamburger click smoothly collapses/expands the sidebar with content reflow; mobile loads with the sidebar off-screen by default (no flash-then-hide), hamburger opens it as a dimmed overlay drawer, clicking a nav link navigates and auto-closes the drawer, clicking the backdrop also closes it; clicking the "AUDIENCE" heading collapsed its 5 items (chevron rotated) while "OVERVIEW"/"SETTINGS" stayed expanded and unaffected, re-clicking restored it. Commit `ef877c1` |
| GRX-TEST-001 | Testing | Backend test foundation | Test runner config, DB test fixtures/factories, coverage baseline | P0 | DONE | GRX-FOUND-005 (DONE) | coding agent | `apps/api/tests/conftest.py`, `apps/api/pyproject.toml` (pytest-cov, `integration` marker) | `pytest` runs green on a trivial test in CI | CI run | — | 2026-07-25 | 2026-07-25 | `ruff`/`mypy` clean; `pytest` 14 passed with coverage reporting enabled (87% baseline on `growixa_api`); `pytest -m "not integration"` (7 tests) verified to pass with zero DB/Redis/RabbitMQ reachable, confirming the unit/integration split is real, not just labeled. No CI pipeline exists yet — that is `GRX-DEVOPS-001`, which depends on this task; `pytest` running green locally is this task's actual deliverable. Commit `de55382` |
| GRX-TEST-002 | Testing | Frontend test foundation | Test runner config, component test harness, one e2e smoke test | P0 | DONE | GRX-FOUND-004 (DONE) | coding agent | `apps/web/vitest.config.ts`, `apps/web/vitest.setup.ts`, `apps/web/playwright.config.ts`, `apps/web/src/app/page.test.tsx`, `apps/web/tests/e2e/smoke.spec.ts` | Test suite runs green on a trivial test in CI | CI run | — | 2026-07-27 | 2026-07-27 | Picked ahead of `GRX-FOUND-008` because that task's own "Required Tests" (frontend e2e smoke test) can't be produced without a test runner existing first — same reasoning as why `GRX-TEST-001` preceded most backend feature work; `npm run lint`/`format:check`/`typecheck` all pass; `npm run test` (Vitest + React Testing Library, jsdom) → 1 passed (renders `HomePage`'s heading); `npm run test:e2e` (Playwright, Chromium) → 1 passed (root route returns 200, heading visible) against a real `next build && next start`; rebuilt `web` image with the new devDependencies, confirmed `/` still 200 and unknown route still 404 in Compose. No CI pipeline exists yet — that is `GRX-DEVOPS-001`, which depends on this task; the test suite running green locally is this task's actual deliverable (mirrors `GRX-TEST-001`'s equivalent evidence). Commit `a804186` |
| GRX-DEVOPS-001 | DevOps | CI pipeline | Lint, format check, type check, backend tests, frontend tests, Alembic migration check on every PR | P0 | DONE | GRX-TEST-001 (DONE), GRX-TEST-002 (DONE) | coding agent | `.github/workflows/ci.yml` | CI passes on a clean baseline branch | CI run | — | 2026-07-27 | 2026-07-31 | Implements all 8 stages from `TEST_STRATEGY.md` §CI test stages, split across 3 jobs: `backend` (lint/format/typecheck/`alembic check`/`pytest`, against real GitHub Actions Postgres+Redis+RabbitMQ service containers — Redis is host-reachable in CI, unlike local dev per `GRX-FOUND-006`'s finding, so tests that skip locally actually execute here), `frontend` (lint/format/typecheck/Vitest/`next build`, parallel to `backend`), and `e2e` (`needs: [backend, frontend]` — only stands up the real Compose stack, i.e. `docker compose up postgres redis rabbitmq api` plus Playwright's own webServer for the frontend, once the cheaper checks already passed). Made `apps/web/tests/e2e/{global-setup,global-teardown}.ts` compose-binary-portable (`COMPOSE_BIN` env var, defaults to `podman` for local dev, CI sets `docker`) so the same e2e suite runs unmodified in both environments. Locally re-verified every command the workflow runs: `ruff check`/`ruff format --check`/`mypy` clean; `alembic upgrade head && alembic check` → "No new upgrade operations detected"; `pytest` → 45 passed, 2 skipped; frontend `eslint`/`prettier --check`/`tsc --noEmit`/`vitest run` all clean; `next build` → succeeds, correctly classifies `/` and `/login` static, `/dashboard` dynamic; `npm run test:e2e` → 3 passed with the new `COMPOSE_BIN` parameterization (defaulting to `podman`, unchanged local behavior). Workflow YAML syntax-validated via `python3 -c "import yaml; yaml.safe_load(...)"`. Moved to `DONE`: the user pushed to the remote and confirmed a green GitHub Actions run on `main`. Commit `7ff54dc` |
| GRX-DOC-003 | Documentation | Sprint 1 documentation + handoff update | Update `PROJECT_STATUS.md`, `FEATURE_STATUS_MATRIX.md`, `CHANGELOG.md`, `AGENT_HANDOFF.md` after Sprint 1 completes | P0 | DONE | All Sprint 1 tasks above (all DONE) | coding agent | `docs/00-project-control/{PROJECT_STATUS,FEATURE_STATUS_MATRIX,CHANGELOG,AGENT_HANDOFF}.md` | All Sprint 1 acceptance criteria in [SPRINT_01_FOUNDATION.md](../14-sprints/SPRINT_01_FOUNDATION.md) verified and recorded | N/A (documentation) | — | 2026-07-31 | 2026-07-31 | Created `FEATURE_STATUS_MATRIX.md` (new) — a per-feature status table distinct from `FEATURE_CATALOG.md`'s planning list, cross-checked against actual code (not just tracker claims) for every Slice 1/2 feature. This surfaced one real gap: `GRX-FEAT-027` (Audit Logs) is `PARTIAL`, not `DONE` — recording works but there's no `GET` endpoint or frontend page, so Sprint 1's "audit logs are visible to users with `audit.view`" acceptance criterion was never actually satisfied. Filed as new `GRX-AUDIT-002` (`BACKLOG`) rather than silently marking Sprint 1 fully clean. Also confirmed Notifications/Usage Metering/Integrations/Admin Portal (catalog-tagged "Slice 1") were never in Sprint 1's actual task breakdown and remain `NOT_STARTED` — the catalog's slice tags are aspirational, not a claim those were delivered. Updated `PROJECT_STATUS.md` to reference the new matrix and reflect `GRX-DEVOPS-001`/`GRX-DOC-003` both closing out Sprint 1. Updated `CHANGELOG.md` and `AGENT_HANDOFF.md` accordingly. Sprint 1 is now genuinely fully accounted for, gaps included. Commit `2cc3acc` |
| GRX-CONTACT-001 | Contacts | Contacts schema + CRUD | `contacts`, `contact_custom_fields`, `contact_field_values` tables; create/edit/archive API with email dedup; `contacts.manage`/`contacts.view` permission seed | P0 | DONE | GRX-FOUND-005 (DONE), GRX-RBAC-001 (DONE), GRX-AUTH-001 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/`, migration `209d29349ccf`, `apps/api/tests/test_contacts.py` | Admin can create, edit, archive a contact; duplicate email updates instead of duplicating; Viewer-equivalent (`contacts.view` only) cannot write | Integration tests for CRUD + dedup + permission split | — | 2026-07-30 | 2026-07-30 | `POST /contacts` (create-or-update-by-email), `GET /contacts`, `GET /contacts/{id}`, `PATCH /contacts/{id}`, `PATCH /contacts/{id}/status`, plus `GET`/`POST /contacts/custom-fields`. Email is the sole dedup key — creating with an existing email updates that row instead of duplicating (verified live: same id returned, list stays at 1). Contact activity reuses `audit_logs` (`contact.created`/`updated`/`archived`) rather than a new table. `contacts.manage` → Super Admin/Admin/Marketing Manager; `contacts.view` → those three plus Analyst (Viewer gets neither, per RBAC.md's Slice 2 rationale). Found and fixed a real bug during live verification: `updated_at`'s server-side `onupdate` gets expired after `session.commit()`, and reading it synchronously afterward raised `MissingGreenlet`; fixed with an explicit `await session.refresh(contact)` post-commit in every mutating service function. Updated `test_auth_schema_seed.py`'s exact permission-set assertion to include the two new codes (a legitimate extension, not a regression). `ruff`/`mypy` clean; `pytest` 69 passed, 3 skipped (94% coverage, 12 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: created a contact with a custom field, re-created with the same email (dedup confirmed via matching id + list count), archived/reactivated (audit event confirmed), and confirmed Analyst gets 200 on GET/403 on POST while Viewer gets 403 on both. Commit `7ec6c93` |
| GRX-CONTACT-002 | Contacts | Tags & lists | `tags`, `contact_tags`, `contact_lists`, `contact_list_members` tables + API | P0 | DONE | GRX-CONTACT-001 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `788ff9dd33db`, `apps/api/tests/test_contacts_tags_and_lists.py` | Contacts can be tagged and added to a manually curated list | Integration tests | — | 2026-07-30 | 2026-07-30 | Added `tags`/`contact_tags` and `contact_lists`/`contact_list_members` tables. `GET`/`POST /contacts/tags`, `POST`/`DELETE /contacts/{id}/tags[/{tag_id}]` (attach/detach), `GET`/`POST /contacts/lists`, `GET /contacts/lists/{id}`, `POST`/`DELETE /contacts/lists/{id}/members[/{contact_id}]`. `ContactOut` now includes `tags: list[str]`; `ContactListOut` includes a live `member_count`. Attach/detach and list-membership changes emit `contact.tagged`/`contact.list_added` audit events (reusing `audit_logs`, per the Slice 2 design decision — no new activity table). No RBAC changes needed — `contacts.manage`/`contacts.view` already covered tags/lists per RBAC.md's Slice 2 description. `ruff`/`mypy` clean; `pytest` 76 passed, 3 skipped (94% coverage, 7 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: created a tag and attached/detached it from a contact (tags array updated both times), created a list and added/removed a member (member_count went 0→1→0), confirmed Analyst gets 200 on GET/403 on POST for both tags and lists while Viewer gets 403 on both. `GRX-CONTACT-003` (segments) is now the next `READY` task. Commit `1e69461` |
| GRX-CONTACT-003 | Contacts | Segments | `segments`, `segment_rules`, `segment_members` tables + API; AND-only rule evaluation; DYNAMIC live-evaluated, SAVED frozen snapshot | P0 | DONE | GRX-CONTACT-002 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `18cf808f2b87`, `apps/api/tests/test_contacts_segments.py` | A segment's membership can be computed from its rules (DYNAMIC) or frozen at save time (SAVED) | Integration tests for both segment types | — | 2026-07-30 | 2026-07-31 | Added `segments`/`segment_rules`/`segment_members` tables and a rule evaluator supporting `status`/`email`/`source` (equals; email also contains), `tag` (equals), `created_at` (before/after), and `custom_field:<key>` (equals/contains) — all AND-combined only, per the Slice 2 scope decision. `consent_status` was named as an example field in early planning but isn't supported yet since `consent_records` doesn't exist until GRX-CONTACT-005; using it now correctly 400s as an unsupported field rather than silently matching nothing. `POST /contacts/segments` validates every rule (unknown field/operator/custom-field-key/unparseable date all 400) before creating anything. `DYNAMIC` segments compute membership live on every read; `SAVED` segments evaluate once at creation and freeze into `segment_members` — verified live that tagging a new contact after creation changes a DYNAMIC segment's count but not a SAVED one's. `ruff`/`mypy` clean; `pytest` 85 passed, 3 skipped (93% coverage, 9 new tests); `alembic check` → no drift (had to add `index=True` to `SegmentRule.segment_id` in the ORM model to match the migration's explicit index, caught by `alembic check` itself). Live-verified against rebuilt Compose containers: created a DYNAMIC and a SAVED segment against the same rule, tagged a second contact afterward, confirmed DYNAMIC's count went 1→2 while SAVED stayed at 1; confirmed Analyst gets 200 on GET/403 on POST, Viewer gets 403. This was the last backend-only Slice 2 task — `GRX-CONTACT-004` (CSV import) is next `READY`. Commit `a725e0e` |
| GRX-CONTACT-004 | Contacts | CSV contact import | `contact_imports`, `contact_import_rows` tables + API; upload, column mapping, validation, per-row status, import history | P0 | DONE | GRX-CONTACT-002 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `b11cffc2cbcf`, `apps/api/tests/test_contacts_import.py` | A CSV file can be imported, producing a history entry with imported/skipped/error counts; duplicate emails update existing contacts | Integration tests incl. malformed rows and duplicate-email handling | — | 2026-07-31 | 2026-07-31 | Added `contact_imports`/`contact_import_rows` tables. `POST /contacts/imports` accepts multipart upload (`file` + a `column_mapping` JSON string mapping CSV headers to `email`/`first_name`/`last_name`/`phone`/`source`/`custom_field:<key>`); processes synchronously and returns imported/updated/skipped/error counts. Row outcomes: blank rows → `SKIPPED`, missing email → `ERROR`, unknown custom-field key → 400 at mapping-validation time (before any row is processed) or `ERROR` per-row if the key check races; existing-email rows reuse `create_or_update_contact`'s dedup logic and are marked `UPDATED`, new emails `IMPORTED`. `GET /contacts/imports`, `GET /contacts/imports/{id}`, `GET /contacts/imports/{id}/rows` expose history and per-row detail. No new permissions — reuses `contacts.manage`/`contacts.view`. Added `python-multipart` dependency for FastAPI's `UploadFile`/`Form` handling, and extended ruff's `flake8-bugbear` immutable-calls allowlist with `fastapi.File`/`fastapi.Form`. `ruff`/`mypy` clean; `pytest` 93 passed, 3 skipped (8 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: uploaded a 3-row CSV (one new email, one pre-existing email, one blank email) mapped to `email`/`first_name`/`custom_field:plan` — got back `imported_count=1`, `updated_count=1`, `error_count=1`, confirmed per-row detail matched, confirmed the new contact carried its custom field and the existing contact's first name and custom field were both updated, confirmed a mapping without an `email` target 400s. This closes out Sprint 2's backend CSV import slice — `GRX-CONTACT-005` (consent & suppression) is next `READY`. Commit `52fb417` |
| GRX-CONTACT-005 | Contacts | Consent & suppression | `consent_records` (insert-only), `suppression_entries` (upsert-on-email) tables + API | P0 | DONE | GRX-CONTACT-001 (DONE) | coding agent | `apps/api/src/growixa_api/contacts/` (extended), migration `97642610fb46`, `apps/api/tests/test_contacts_consent_and_suppression.py` | Consent per channel can be recorded and its history viewed; an email can be suppressed and re-suppression updates in place | Integration tests | — | 2026-07-31 | 2026-07-31 | Added `consent_records`/`suppression_entries` tables exactly per DATA_MODEL.md's field-level spec. `POST`/`GET /contacts/{id}/consent` record and list an insert-only consent history (channel `EMAIL`/`SMS`, status `GRANTED`/`WITHDRAWN`/`UNKNOWN`), newest-first — current status per channel is derived as "most recent row," never stored as separate mutable state. `POST /contacts/suppression` upserts on the unique `email` index: a new email creates a row, an already-suppressed email updates `reason`/`suppressed_at` in place (same id, no duplicate) — verified live both ways. `contact_id` is optional on suppression (an address can be suppressed with no matching contact, e.g. a hard bounce) but is validated to exist when given. Per the sprint's explicit acceptance criterion ("visibly flagged as suppressed wherever contacts are shown"), `ContactOut` gained `is_suppressed: bool`, computed from the suppression table at snapshot time — this widened `ContactSnapshot` to a 4-tuple across every contacts service function and `_to_out` call site in `api.py`. No new permissions — reuses `contacts.manage`/`contacts.view`, per RBAC.md's existing scope note ("record consent; suppress addresses" under `contacts.manage`). `suppressed_at` deliberately has no `onupdate` trigger — the upsert path sets it explicitly in Python, avoiding this session's known `MissingGreenlet` pattern entirely rather than needing a `session.refresh()`. `ruff`/`mypy` clean; `pytest` 101 passed, 3 skipped (8 new tests); `alembic check` → no drift. Live-verified against rebuilt Compose containers: recorded GRANTED then WITHDRAWN consent for a contact (history returned newest-first, 2 rows); suppressed a contact's email (contact's `is_suppressed` flipped true), re-suppressed with a different reason (same suppression id, reason updated, still exactly one list entry); suppressed an email with no contact at all (contact_id null); confirmed an unknown `contact_id` 404s. This closes out Sprint 2's entire backend slice (contacts, tags, lists, segments, CSV import, consent/suppression) — `GRX-CONTACT-006` (contacts frontend) is next `READY`. Commit `30b5fc2` |
| GRX-CONTACT-006 | Contacts | Contacts frontend | List/detail/create/edit UI for contacts, gated on `contacts.manage`/`contacts.view` | P1 | DONE | GRX-CONTACT-001 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/dashboard/contacts/`, `apps/web/src/app/dashboard/sidebar.tsx`, `apps/web/src/app/dashboard/page-title.tsx` | Admin can manage contacts through the UI; a view-only user sees the same data with no write controls | Frontend component tests | — | 2026-07-31 | 2026-07-31 | Built `ContactsPage` (list + inline "+ Add contact" form + an expandable per-row detail/edit panel), gated page visibility on `contacts.view` and write controls (create form, edit fields, archive/activate) on `contacts.manage`. The detail panel shows created/updated timestamps, read-only tag chips and custom fields (tag/segment assignment UI is `GRX-CONTACT-007`'s scope, not this task's), and the `is_suppressed` flag as a badge on both the row and (implicitly) the detail view. Added a new "AUDIENCE" sidebar section with the "Contacts" nav item (gated on `contacts.view` — sufficient since every role with `contacts.manage` also has `contacts.view` per RBAC.md's role matrix) and a page-title entry. `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest` 19 passed (6 new component tests covering list rendering, access-denied, view-only hides write controls, create, edit, archive). Live-verified in the browser against the rebuilt dev server: logged in as Super Admin, created a contact, edited its name inline, archived then re-activated it (toast confirmations on each), then logged in as a fresh Analyst (`contacts.view` only) and confirmed the same contact was visible with no "+ Add contact" button and a "You have view-only access to contacts." note in place of the edit form. This is the first of Sprint 2's four remaining frontend tasks — `GRX-CONTACT-007`/`008`/`009` all now have their dependencies satisfied and were flipped to `READY`. Commit `07550c0` |
| GRX-CONTACT-007 | Contacts | Tags/lists/segments frontend | UI for tag/list assignment and segment builder (rule editor) | P1 | DONE | GRX-CONTACT-002 (DONE), GRX-CONTACT-003 (DONE), GRX-CONTACT-006 (DONE) | coding agent | `apps/web/src/app/dashboard/contacts/` (extended: tag assignment in the detail panel; new `lists/` and `segments/` subpages), `sidebar.tsx`, `page-title.tsx` | Admin can tag contacts, manage lists, and build/save a segment through the UI | Frontend component tests | — | 2026-07-31 | 2026-07-31 | Extended `ContactsPage`'s detail panel with tag management: removable chips (`×` calling `DELETE /contacts/{id}/tags/{tag_id}`, resolving tag name→id via a fetched `/contacts/tags` list since `ContactOut.tags` is names-only), a select to attach an existing tag, and an inline "+ New tag" mini-form that creates then attaches in one step. Built `ListsPage` (`/dashboard/contacts/lists`): create a list, and per-list add/remove-by-contact-picker controls — no member-browsing UI, since the backend only ever exposed `member_count`, not a members-list endpoint for `contact_lists` (unlike segments); extending that was judged out of this task's stated frontend-only scope, so the UI is honest about what the API can show. Built `SegmentsPage` (`/dashboard/contacts/segments`): a rule builder (field/operator/value rows, operators filtered per field to match `SEGMENT_RULE_FIELD_OPERATORS`, a `custom_field:<key>` sub-input when "Custom field" is picked), segment list with type/member-count badges and a rule-summary bullet list, and "View members" using the existing `GET /contacts/segments/{id}/members`. Refactored `contacts-page.module.css` into a shared `shared.module.css` so all three pages (contacts, lists, segments) reuse one set of card/form/row/badge styles instead of duplicating them. Added "Lists" and "Segments" to the sidebar's AUDIENCE section (gated `contacts.view`) and page-title entries. `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest` 32 passed (13 new: 3 tag-assignment tests on `ContactsPage`, 5 on `ListsPage`, 5 on `SegmentsPage`). Live-verified against the rebuilt dev server: created a tag inline and attached it to a contact, detached and reattached it via the dropdown; created a list and added a contact to it (count 0→1); created a DYNAMIC segment on `tag equals VIP` and confirmed it matched the tagged contact (member_count 1, "View members" showed the right email); confirmed an Analyst sees all three pages' data but no write controls anywhere (no "+ Add list"/"+ Add segment", "You have view-only access to lists/segments." notes, tag chips with no `×`). This closes GRX-CONTACT-007 — `GRX-CONTACT-008` (CSV import frontend) and `GRX-CONTACT-009` (consent/suppression frontend) remain `READY`, no ordering dependency between them. Commit `262d28a` |
| GRX-CONTACT-008 | Contacts | CSV import frontend | Upload UI, column-mapping step, import-history view | P1 | DONE | GRX-CONTACT-004 (DONE), GRX-CONTACT-006 (DONE) | coding agent | `apps/web/src/app/dashboard/contacts/imports/` (new), `sidebar.tsx`, `page-title.tsx`, `lib/api-client.ts` (FormData support) | Admin can upload a CSV, map columns, and see import results in the UI | Frontend component tests | — | 2026-07-31 | 2026-07-31 | Built `ImportsPage` (`/dashboard/contacts/imports`): a file picker reads the CSV's header row client-side (`FileReader` + naive comma-split — full parsing/validation is the backend's job, this is only for building the mapping UI), auto-guesses common column names (email/first_name/last_name/phone/source) via exact-match heuristics, then renders one field-target select per column (including `custom_field:<key>` options sourced from `GET /contacts/custom-fields`); submitting builds a `column_mapping` JSON string and multipart-POSTs to `POST /contacts/imports`. This was the frontend's first-ever file upload, which exposed that `apiFetch` unconditionally set `Content-Type: application/json` — fixed by skipping that header when `init.body instanceof FormData`, letting the browser set its own multipart boundary; added `api-client.test.ts` (new, 2 tests) since this shared utility had no prior coverage. Below the upload form, an import-history list (`GET /contacts/imports`) shows filename/status/counts per past import, each expandable via "View rows" (`GET /contacts/imports/{id}/rows`) into per-row email/status/error_message detail. Page visibility gated on `contacts.view`; the upload card is omitted entirely (not just disabled) for non-managers. Added "Imports" to the sidebar's AUDIENCE section and a page-title entry. `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest` 39 passed (7 new: 2 on `apiFetch`, 5 on `ImportsPage` covering history rendering, access-denied, view-only hides the upload form, header-parsing/mapping/submit via `userEvent.upload`, and row-detail viewing). Live verification note: the browser-automation tool cannot drive a native OS file-picker dialog (browsers block scripting `<input type=file>.value` for security), so the actual "select a file via the picker" step could not be exercised through the browser; instead, uploaded a real 3-row CSV via `curl` multipart (mirroring exactly what `fetch`+`FormData` sends) — got back `imported_count=2`, `error_count=1` — then reloaded the page in the browser and confirmed the history entry, counts, and "View rows" row-level detail (including the blank-email error row) all rendered correctly from real backend data; confirmed the two imported contacts appeared correctly on the Contacts page with mapped names; confirmed a fresh Analyst sees only the history card with no upload form at all. This closes GRX-CONTACT-008 — `GRX-CONTACT-009` (consent/suppression frontend) remains `READY`, the last Sprint 2 task. Commit `7796f93` |
| GRX-CONTACT-009 | Contacts | Consent/suppression frontend | UI to view consent history and manage the suppression list | P1 | DONE | GRX-CONTACT-005 (DONE), GRX-CONTACT-006 (DONE) | coding agent | `apps/web/src/app/dashboard/contacts/suppression/` (new), `contacts-page.tsx`/`types.ts` (extended), `sidebar.tsx`, `page-title.tsx` | Admin can view a contact's consent history and add/remove a suppression entry through the UI | Frontend component tests | — | 2026-07-31 | 2026-07-31 | Extended `ContactsPage`'s detail panel with a lazy-loaded (on row expand) consent history list (`GET /contacts/{id}/consent`, newest-first) and, for managers, an inline record-consent form (channel/status selects + optional source input) posting to `POST /contacts/{id}/consent`. Built `SuppressionPage` (`/dashboard/contacts/suppression`): a list of all suppression entries (email, linked contact's email or "No matching contact", reason badge, timestamp) and, for managers, a "+ Suppress an email" form (email, reason select, optional contact picker sourced from `GET /contacts`) posting to `POST /contacts/suppression`; since the backend has no unsuppress endpoint by design (`GRX-CONTACT-005`), the UI never offers a remove control. Re-suppressing an already-suppressed email is handled by matching the POST response's `id` against existing state and replacing in place rather than appending, mirroring the backend's upsert-on-email semantics — covered by a dedicated test and confirmed live. Added "Suppression" to the sidebar's AUDIENCE section (gated `contacts.view`) and a page-title entry. `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest` 46 passed (7 new: 2 consent-history/record tests on `ContactsPage`, 5 on `SuppressionPage` covering list rendering, access-denied, view-only hides the suppress form, add, and upsert-in-place). Live-verified against the rebuilt dev server: recorded GRANTED then WITHDRAWN consent for a contact and confirmed newest-first ordering; suppressed an email with a linked contact (contact's Contacts-page row correctly flipped to "Suppressed"), re-suppressed the same email with a different reason and confirmed the entry updated in place (still exactly one row); confirmed a fresh Analyst sees both the consent history and suppression list read-only with no record-consent form and no "+ Suppress an email" button. This closes GRX-CONTACT-009 — **all of Sprint 2 (GRX-CONTACT-001 through 009) is now DONE.** Commit `81332b5` |

| GRX-EMAIL-001 | Email | Email provider connection + sender identity | `email_provider_connections`/`sender_identities` tables + API; Fernet credential encryption | P0 | DONE | GRX-FOUND-005 (DONE), GRX-RBAC-001 (DONE) | coding agent | `apps/api/src/growixa_api/integrations/` (new), `apps/api/src/growixa_api/auth/encryption.py` (new), migration `393c4222c8af`, `apps/api/src/growixa_api/config.py` (`encryption_key`) | Super Admin can save a Postmark SMTP connection and a sender identity; the stored password is encrypted, never returned in plaintext by any response | Integration tests incl. encryption round-trip and non-Super-Admin 403 | — | 2026-08-03 | 2026-08-03 | First task of Sprint 3. Added a Fernet symmetric-encryption utility (`auth/encryption.py`, `encrypt_secret`/`decrypt_secret`, mirroring `auth/security.py`'s `@lru_cache` hasher pattern) and a new `encryption_key` setting — a genuinely generated `Fernet.generate_key()` value for local dev, distinct from `jwt_signing_key` since it must be reversible (the worker will need to decrypt SMTP credentials to send mail). New `integrations` module (named per `MODULE_BOUNDARIES.md` — deliberately not `email_integrations`, since this module is the designated home for all future provider connections, not just email) with `EmailProviderConnection`/`SenderIdentity` models, `integrations.manage` permission (Super Admin only — the project's first Admin-excluded permission). `EmailProviderConnectionOut` never exposes the password or its encrypted form. Creating a new connection deactivates any existing active one rather than mutating it in place. Migration `393c4222c8af` adds both tables plus the partial `(is_active) WHERE is_active` index and seeds `integrations.manage`; `alembic check` confirmed no drift after also declaring the index on the SQLAlchemy model. **Real bug found and fixed**: the sender-identity status-update route hit `MissingGreenlet` on `updated_at` after commit — `onupdate=func.now()` columns expire after an UPDATE and need an explicit `await session.refresh()` before serialization (same root cause and fix already documented in `contacts/services.py`); moved the commit+refresh into the service function to match that established pattern. Also aligned the invalid-status error response to `400` (not `422`, which isn't used anywhere else in this codebase). `ruff`/`mypy` clean; `pytest` 111 passed, 3 skipped (7 new integration tests incl. encryption round-trip and Admin-gets-403); updated `test_auth_schema_seed.py`'s expected permission set/matrix for the new `integrations.manage` row; `alembic check` clean. Rebuilt the `api` container (new `cryptography` dependency baked into the image). Live-verified against Compose: Super Admin created a real connection + sender identity via curl, password never appeared in any response body and decrypted correctly from the DB; a throwaway Admin-role user got 403 on both GET and POST; recreated `admin@growixa.local` and cleaned up all smoke-test rows afterward (running the full suite wipes it again, per this file's earlier `GRX-CONTACT-*` entries). Commit `f545ad0` |
| GRX-EMAIL-002 | Email | Email templates + versioning | `email_templates`/`email_template_versions` tables + API; insert-only version history | P0 | DONE | GRX-EMAIL-001 (DONE) | coding agent | `apps/api/src/growixa_api/templates/` (new), migration `d36211c53aed` | A template can be created and edited; editing produces a new version, never mutates a past one | Integration tests | — | 2026-08-03 | 2026-08-03 | New `templates` module: `EmailTemplate`/`EmailTemplateVersion` models matching `DATABASE_SCHEMA.md`'s spec, gated on `campaigns.manage` (write) / `campaigns.view` (read) per RBAC.md — these two permissions didn't exist yet (Slice 3 planning assigned them to whichever task first needed them), so migration `d36211c53aed` seeds both, deferring `campaigns.send` to GRX-EMAIL-004 where the send action actually lands. `POST /templates` creates a template plus version 1 in one call (a template can't exist without content); `POST /templates/{id}/versions` appends version `max+1` — the previous version's row is never touched, matching `ConsentRecord`'s insert-only precedent, with "current" derived as the highest `version_number` rather than a mutable pointer. `alembic check` clean, no drift. `ruff`/`mypy` clean; `pytest` 117 passed, 3 skipped (6 new integration tests: create, edit-creates-new-version-without-mutating-old, `campaigns.view`-only role can read but not write, Viewer gets 403 on read, unauthenticated 401, 404 on an unknown template); extended `test_auth_schema_seed.py` for the two new permission codes. Restarted the `api` container (no new dependency this time, code + migration only). Live-verified against Compose: Super Admin created a template via curl, edited it (version_number went 1→2, both versions still present and byte-identical to what was sent), a throwaway Viewer got 403 on `GET /templates`; cleaned up all smoke-test rows afterward. Commit `1dac3ff` |
| GRX-EMAIL-003 | Email | Campaigns CRUD + targeting | `campaigns`/`campaign_versions`/`campaign_recipients` tables + API; segment/list/all-contacts targeting | P0 | DONE | GRX-EMAIL-001 (DONE), GRX-EMAIL-002 (DONE), GRX-CONTACT-002 (DONE), GRX-CONTACT-003 (DONE) | coding agent | `apps/api/src/growixa_api/campaigns/` (new), migration `d7fa144e5c60` | A campaign draft can be created targeting a segment, list, or all contacts, referencing a template or ad hoc content; draft content is editable until send | Integration tests | — | 2026-08-03 | 2026-08-03 | New `campaigns` module. Migration `d7fa144e5c60` creates all three Slice 3 tables named in this task (`campaigns`, `campaign_versions`, `campaign_recipients`) since `DATABASE_SCHEMA.md`'s migration-order note groups them together, but only `campaigns` gets CRUD here — `campaign_versions`/`campaign_recipients` exist as schema only, populated later by `GRX-EMAIL-004`'s send pipeline (same schema-now/write-path-later split already used for `GRX-AUDIT-001`→`GRX-AUDIT-002`). No new permissions needed; reused `campaigns.manage`/`campaigns.view` seeded by `GRX-EMAIL-002`. Recipient targeting (`SEGMENT`/`LIST`/`ALL_CONTACTS`, exactly one of `recipient_segment_id`/`recipient_list_id` set per type) is validated at the service layer per `DATABASE_SCHEMA.md`'s note that this isn't a DB CHECK constraint; referenced `sender_identity_id`/`template_id`/segment/list are validated for existence by calling directly into `integrations`/`templates`/`contacts`' own repository functions, per `MODULE_BOUNDARIES.md`'s cross-module call convention. Draft editing (`PATCH`) uses `exclude_unset` so an update can explicitly null a field (e.g. clearing `recipient_segment_id` when switching `recipient_type` to `ALL_CONTACTS`) — the first module in this codebase to need that distinction over the simpler "`None` means don't touch" convention used elsewhere. Editing is blocked with 409 once `status` leaves `DRAFT`, enforced at the service layer as specified. `alembic check` clean; `ruff`/`mypy` clean; `pytest` 127 passed, 3 skipped (10 new integration tests: create for each recipient_type, invalid targeting shapes incl. unknown segment/both-set/missing, unknown sender_identity 404, edit updates fields and switches targeting cleanly, edit blocked once non-DRAFT (409), `campaigns.view`-only role reads but can't write, unauthenticated 401, 404 on unknown campaign). Restarted the `api` container (no new dependency). Live-verified against Compose: Super Admin created a segment-targeted campaign via curl, edited it to `ALL_CONTACTS` and confirmed `recipient_segment_id` was cleared, an invalid both-set payload got 400, a throwaway Viewer got 403; cleaned up all smoke-test rows afterward. Commit `52fa336` |
| GRX-EMAIL-004 | Email | Send pipeline (worker + email_delivery) | `message_deliveries`/`delivery_attempts` tables + `grx.email_delivery.send_campaign` job; test send + immediate send | P0 | DONE | GRX-EMAIL-003 (DONE), GRX-FOUND-007 (DONE), GRX-CONTACT-005 (DONE) | coding agent | `apps/api/src/growixa_api/email_delivery/` (new), `apps/api/src/growixa_api/usage/` (new), `apps/worker/` (extended: db.py, models.py, encryption.py, email_sender.py, recipients.py, send_campaign.py), migrations `7049ac70cac8` | Sending enqueues a worker job (not inline); recipients are resolved and suppression-checked at send time; a `campaign_versions` snapshot is frozen; `usage_records` gets its first real write | Integration tests incl. suppression-bypass negative test | — | 2026-08-03 | 2026-08-04 | Largest Sprint 3 task so far. **Real gap discovered and fixed**: `usage_records` was documented (`DATA_MODEL.md`, `DEC-GRX-007`) as already existing since Sprint 1, but no migration/model for it existed anywhere in the codebase — created it here (new `usage` module) since this task's own AC needed it as a genuine first writer. Per the user's explicit decision, `apps/worker` got its own minimal SQLAlchemy/asyncpg data layer (models for exactly the tables `send_campaign` touches, including a duplicated segment-rule evaluator) rather than depending on `growixa_api` as a library, keeping the two apps independently deployable per `SYSTEM_ARCHITECTURE.md`. New `campaigns.send` permission (Super Admin/Admin/Marketing Manager only, Content Creator excluded) seeded in the same migration as the new tables. `POST /campaigns/{id}/test-send` sends synchronously and inline (explicitly allowed since it touches no campaign/recipient/delivery state, unlike a real send); `POST /campaigns/{id}/send` flips the campaign to `SENDING` and enqueues `grx.email_delivery.send_campaign`, never sending inline. The worker's job handler resolves recipients per targeting type (including live evaluation of `DYNAMIC` segment rules), excludes addresses on `suppression_entries` or with withdrawn `EMAIL` consent (marking them `SUPPRESSED`, per `DEC-GRX-008`), freezes exactly one `campaign_versions` snapshot, sends via real `aiosmtplib`, writes `message_deliveries`/`delivery_attempts`, and writes one `usage_records` row (`operation_type='email.sent'`, `quantity`=successfully-sent count). Idempotency is enforced by checking for an existing `campaign_versions` row before doing any work, rather than a separate key-tracking store. `ruff`/`mypy` clean on both `apps/api` and `apps/worker`; discovered and fixed apps/worker's own pytest-asyncio config gap (missing `asyncio_default_fixture_loop_scope`/`asyncio_default_test_loop_scope = "session"`, present in `apps/api` since GRX-TEST-001 but never added here — its absence gave each test a fresh event loop, breaking the worker's cached DB engine across tests) while debugging the new tests. `apps/api` `pytest`: 135 passed, 3 skipped (8 new integration tests incl. the new Content-Creator-has-manage-not-send permission-gated-action-within-a-resource test shape). `apps/worker` `pytest`: 8 passed (recipient resolution for all three targeting types incl. dynamic segment rules, suppression/withdrawn-consent exclusion, SMTP-failure handling, reprocessing idempotency). `alembic check` clean on both. Rebuilt both `api` and `worker` containers (new dependencies: `aiosmtplib` on `api`; `sqlalchemy`/`asyncpg`/`cryptography`/`aiosmtplib` on `worker`, which needed its own `.env`/`.env.example` for the first time). **Evidence gap, per DEC-GRX-011 / SPRINT_03's own pre-approved fallback**: no live Postmark account is available in this environment, so the actual outbound send was never verified as *succeeding*. Live-verified everything short of that: a real TCP/TLS connection and STARTTLS handshake against Postmark's actual SMTP relay (`smtp.postmarkapp.com:587`), which rejected the placeholder credentials with a genuine `535 5.7.8 authentication failed` — proving the SMTP client code path is correct end-to-end, not mocked, right up to the credential boundary. The worker correctly caught that failure, marked the recipient/delivery `FAILED`, still wrote the `campaign_versions` snapshot and a `usage_records` row (`quantity=0`), and left the campaign `SENT` rather than crashing. This gap closes automatically the first time a real Postmark server token is configured. Commit `aa2bdb4` |
| GRX-EMAIL-005 | Email | Postmark webhook receiver + unsubscribe handling | `email_events`/`unsubscribe_events` tables + authenticated webhook endpoint | P0 | DONE | GRX-EMAIL-004 (DONE) | coding agent | `apps/api/src/growixa_api/email_delivery/` (extended), `apps/api/src/growixa_api/integrations/` (extended), `apps/worker/src/growixa_worker/send_campaign.py` (extended), migration `f5ecaa79863b` | A valid Postmark webhook updates delivery status and records an event; an unauthenticated request is rejected before touching either table; an unsubscribe click suppresses the address | Integration tests incl. Basic Auth rejection negative test | — | 2026-08-05 | 2026-08-05 | **Real gap found and fixed**: `THREAT_MODEL.md`'s `T14` specified webhook Basic Auth credentials "stored alongside the provider connection, encrypted at rest", but `email_provider_connections` never got those columns when Sprint 3 was planned. Migration `f5ecaa79863b` adds `webhook_username`/`webhook_password_encrypted` (nullable, fails closed if unset), auto-generated via `secrets.token_urlsafe` at connection-creation time and Fernet-encrypted like the SMTP password; the plaintext webhook password is returned exactly once, in the create-connection response, never persisted or retrievable again. Same migration adds `email_events` (insert-only webhook ledger, `event_type` CHECK'd to `DELIVERED`/`OPENED`/`CLICKED`/`BOUNCED`/`COMPLAINED`) and `unsubscribe_events` per `DATABASE_SCHEMA.md`. `POST /webhooks/postmark` (public, HTTP Basic Auth checked against the active connection's own credentials via `secrets.compare_digest`) maps Postmark's `RecordType` to `event_type`, matches the delivery by `provider_message_id`, silently no-ops on an unmatched `MessageID` (Postmark expects 200 either way — a redelivery race or a message this system didn't send), and auto-suppresses on `BOUNCED`/`COMPLAINED` — wiring up `suppression_entries.reason` values that have existed since Slice 2 (`GRX-CONTACT-005`) but were never written until now. `GET /unsubscribe/{campaign_recipient_id}` is fully public and unauthenticated by design, identified only by the unguessable UUID (same security shape as the invitation-accept token); it records an `unsubscribe_events` row and suppresses the address, reusing Slice 2's suppression infrastructure with no `actor_id` (a new `_upsert_suppression` helper bypasses `contacts.services.suppress_email`'s actor-requiring wrapper for this and the webhook's system-triggered case). The worker now appends a per-recipient unsubscribe footer link (`{api_public_url}/unsubscribe/{campaign_recipient_id}`) to every real send's `body_html`/`body_text` — no merge-tag infrastructure exists yet, so it's a plain append, not a `{{unsubscribe_url}}` substitution; `api_public_url` is a new worker setting (Compose default `http://localhost:${API_PORT}`). **Documented evidence gap (DEC-GRX-011 / SPRINT_03's pre-approved fallback), same class as `GRX-EMAIL-004`'s**: no live Postmark account is available, so the exact webhook JSON payload shape per `RecordType` is unverified. `PostmarkWebhookPayload` is deliberately permissive (`extra="allow"`, only `RecordType`/`MessageID` required; `Recipient` optional) and the full raw payload is preserved in `email_events.metadata` for future reconciliation once a real payload can be captured. `alembic check` clean; `ruff`/`mypy` clean on both apps; `apps/api` `pytest`: 142 passed, 3 skipped (8 new integration tests: no-credentials/wrong-credentials webhook rejection with zero `email_events` written, a valid `Delivery` event updates `message_deliveries`+writes the event, a `Bounce` event auto-suppresses, an unmatched `MessageID` is a 200 no-op, a valid unsubscribe creates the event+suppression, an unknown `campaign_recipient_id` 404s). `apps/worker` `pytest`: 9 passed (1 new: the unsubscribe URL is present in both `body_html` and `body_text` passed to the mocked `send_email`). Recreated the `admin@growixa.local` smoke-test account (wiped by the full `pytest` run's migration round-trip, per the known quirk). Live-verified against Compose: created a connection and captured its one-time webhook credentials via curl; confirmed no-auth and wrong-auth both 401 before touching `email_events`; sent a real campaign (SMTP `535` auth rejection against Postmark's actual relay, same evidence-gap boundary as `GRX-EMAIL-004`) and manually set `provider_message_id` on the resulting `message_deliveries` row to simulate a Postmark-assigned ID (since the real send never receives one); a `Delivery` webhook flipped the row to `DELIVERED` and wrote the event; a `Bounce` webhook auto-suppressed the recipient (`reason=BOUNCED`); clicking the real `/unsubscribe/{id}` link returned the HTML confirmation, wrote `unsubscribe_events`, and updated the suppression reason to `UNSUBSCRIBED`; an unknown unsubscribe id 404'd. Cleaned up all smoke-test rows (connection, identity, contact, campaign, recipient, delivery, event, suppression) afterward. Commit `9dbe9ee` |
| GRX-EMAIL-006 | Email | Campaign report / analytics endpoint | Aggregate sent/delivered/opened/clicked/bounced/complained counts per campaign | P1 | DONE | GRX-EMAIL-005 (DONE) | coding agent | `apps/api/src/growixa_api/analytics/` (new) | A campaign report reflects the underlying delivery/event data | Integration tests | — | 2026-08-05 | 2026-08-05 | **Corrected the tracker's own planning-time file location**: this row originally said `apps/api/src/growixa_api/campaigns/` (extended), but `MODULE_BOUNDARIES.md` names a dedicated `analytics` module ("Read-side aggregation/reporting over campaigns, posts, AI usage... depends on all modules read-only") for exactly this, and separately `campaigns`' own allowed-dependency list doesn't include `email_delivery` — extending `campaigns` would have required exactly the cross-module import direction the boundaries table forbids. Built a new `analytics` module instead (no models/migration — DEC in `SPRINT_03_EMAIL_CAMPAIGN.md`'s explicitly-excluded list rules out a pre-aggregated `analytics_events` table, so this is pure read-side query), mounted at `GET /campaigns/{id}/report` under the existing `/campaigns` prefix (the same multi-module-same-prefix pattern `email_delivery` already uses for `/send`/`/test-send`), gated by the existing `campaigns.view` permission — `RBAC.md`'s own `campaigns.view` rationale already anticipated this exact use case ("Analyst's 'read-only analytics and reporting' scope is exactly what campaign delivery reports are"). Metric definitions, each independently justified: `sent` counts `campaign_recipients.status = 'SENT'` (set once at send time, never touched by webhook handling, so it stays stable even after a delivery later bounces); `delivered`/`bounced`/`complained` count `message_deliveries.status` (a single mutable pointer reflecting each delivery's current terminal state); `opened`/`clicked` count **distinct** deliveries with a matching `email_events` row rather than raw event rows, since `email_events` is insert-only and a redelivered webhook notification is a new row, not deduplicated — counting rows directly would let a single recipient's repeat notification inflate the number. `ruff`/`mypy` clean; `alembic check` clean (no migration — pure query, no new tables). `pytest`: 147 passed, 3 skipped (5 new integration tests: counts match a hand-built fixture incl. an over-counting check — two `OPENED` events on one delivery still count as one — `campaigns.view`-only role (Analyst) can read, `campaigns.view`-lacking role (Viewer) gets 403, unauthenticated 401, unknown campaign 404). Live-verified against Compose: a freshly created (unsent) campaign's report was all zeros; after a real send (same `535` SMTP evidence-gap boundary as `GRX-EMAIL-004`/`GRX-EMAIL-005`) the recipient's send genuinely failed, so `sent` correctly stayed `0` — a real, honest result, not a fabricated success; separately simulated a Postmark-assigned `provider_message_id` (per the same evidence-gap workaround as `GRX-EMAIL-005`, since a failed send never receives one) and fired real `Delivery`/`Open`/`Open`/`Click` webhook events, after which the report showed `delivered=1, opened=1, clicked=1` — confirming the duplicate `Open` event did not double-count. Cleaned up all smoke-test rows afterward. Commit `ba12931` |
| GRX-EMAIL-007 | Email | Provider connection + sender identity frontend | Settings UI for the Postmark connection and sender identities | P1 | DONE | GRX-EMAIL-001 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/(dashboard)/dashboard/integrations/` (new), `sidebar.tsx`, `page-title.tsx` | Super Admin can configure the connection/sender identity through the UI; every other role sees no UI for it | Frontend component tests | — | 2026-08-05 | 2026-08-05 | New `integrations` dashboard page: a connection form (POST `/integrations/email-provider`) with a one-time webhook-credentials reveal banner (matches `EmailProviderConnectionOut`'s documented one-time convention from `GRX-EMAIL-005`, same UX shape as Team's invite-token banner), a read-only connection summary once one exists, and a sender-identity list + add form (POST `/integrations/sender-identities`) with a manual verification-status dropdown (PATCH `.../status`) per `SPRINT_03`'s note that verification is a manual admin action, not automated polling, in Sprint 3. Sidebar gets a new "Integrations" item gated by `requiresPermission: "integrations.manage"`, matching every other SETTINGS item's pattern. **Real bug found and fixed during live verification, not caught by the component tests as first written**: the connection/sender-identity GET routes are themselves gated by `integrations.manage` on the backend (unlike e.g. `/users`, which any authenticated user can read) — the page's original load effect fetched `/auth/me` and both GETs in one `Promise.all`, so a real non-Super-Admin's 403s on the GETs rejected the whole `Promise.all` and hit the generic `loadError` catch before the `canManage` check was ever reached, so the intended "You don't have access to configure integrations." message never actually rendered — instead surfaced a wrong-but-plausible-looking "Could not load integration settings." This is exactly the kind of bug a mocked component test can hide if the mock doesn't reproduce the real backend's per-endpoint permission gating; fixed by checking `me.permissions` first and only issuing the connection/identity fetches when the user actually has access, and updated the access-denied test to reject those two calls with a real `ApiError(403, ...)` (matching `apiFetch`'s real error type) instead of resolving them, so this exact regression class is now caught by the suite. `eslint`/`tsc --noEmit`/`prettier --check` clean; `vitest`: 59 passed (5 new: access-denied for a role lacking `integrations.manage`, existing connection + identities render, connection creation reveals the one-time webhook credentials, adding a sender identity, changing verification status). `next build` clean, new `/dashboard/integrations` route registered. Live-verified against Compose (after restarting the `web` dev-server container — its file watcher didn't pick up the new route directory on its own over the bind mount): created a real connection as Super Admin and captured the real one-time webhook credentials in the UI, added a sender identity, flipped its verification status to VERIFIED via the dropdown; created a throwaway Admin (not Super Admin) user directly in the database to exercise the negative path — confirmed no "Integrations" item in their sidebar and, after the bug fix above, the correct access-denied message on direct navigation to `/dashboard/integrations`. Cleaned up the smoke-test connection/identity rows; the throwaway Admin account itself was disabled rather than deleted (deleting it would have violated `audit_logs`' insert-only invariant — its login had already written an audit row referencing it). Commit `fa0d924` |
| GRX-EMAIL-008 | Email | Email templates frontend | Template list + dedicated create/edit pages with search/sort/delete/duplicate and a live HTML preview | P1 | DONE | GRX-EMAIL-002 (DONE), GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/(dashboard)/dashboard/templates/` (new: `templates-page.tsx`, `template-form-page.tsx`, `new/page.tsx`, `[id]/edit/page.tsx`), `sidebar.tsx`, `page-title.tsx`, `apps/api/src/growixa_api/templates/{repositories.py,services.py,api.py}` (extended: DELETE endpoint), `apps/api/tests/test_templates.py` (extended), `apps/web/.../company-settings/company-settings-form.module.css` (width fix, see evidence) | A `campaigns.manage` user can create, edit, delete, and duplicate a template through the UI; a view-only user can read, search, and preview but not mutate | Backend integration tests (delete: unused/404/403/409-in-use-by-campaign) + frontend component tests across `templates-page.test.tsx` and `template-form-page.test.tsx` | — | 2026-08-06 | 2026-08-06 | New `templates` dashboard page under a new "CAMPAIGNS" sidebar section (`campaigns.view`-gated), plus two dedicated routes: `/dashboard/templates/new` and `/dashboard/templates/[id]/edit`. **Architecture changed mid-task, twice, both directly from user feedback against reference screenshots**: (1) create/edit started as inline forms on the list page, then moved to dedicated pages after the user found the inline expand-in-place confusing and pointed at a two-pane "New Template" mockup (breadcrumb, form + sticky "Live Preview" panel) — `TemplateFormPage` is one shared client component parameterized by `mode: "create" | "edit"`, reused by both new routes; (2) a card-grid-with-thumbnails reference was also raised, but explicitly scoped down via discussion to "keep the list layout, add real search/sort" — thumbnails, categories/tags, and per-card layout were deferred since categories need a real schema decision, not just UI. Duplicate has no dedicated endpoint — `/dashboard/templates/new?duplicateFrom={id}` fetches the source template client-side and pre-fills the create form (reviewable before saving, not auto-submitted). Delete is a new `DELETE /templates/{id}`: cascade-deletes `email_template_versions` (already `ondelete="CASCADE"`), but `campaigns.template_id`'s FK has no `ON DELETE` behavior — a campaign copies a template's content at creation time rather than depending on it, but the FK still exists to preserve the "created from" link, so deleting a template referenced by a campaign now correctly 409s (`TemplateInUseError`, catches `sqlalchemy.exc.IntegrityError` and rolls back) instead of raising a raw 500; confirmed with an integration test that directly creates a referencing campaign. Frontend confirms via `window.confirm()` (first delete UI in this codebase) and surfaces the backend's real error detail in the toast on failure (same deliberate exception to the canned-message convention used by `GRX-EMAIL-012`'s test-connection button). Search (by name/subject) and sort (by last-updated or name) are pure client-side filters over the already-fetched list — no backend change. Live HTML preview: a `<iframe sandbox="">` (no `allow-scripts`/`allow-same-origin`) renders `srcDoc`, live-verified with a template containing both a `<style>` block and an embedded `<script>` tag — the CSS rendered, the script's DOM injection did not execute. **Also fixed, found live mid-task**: both this page's and Company Settings' cards were capped at a fixed `max-width` (760px / 640px) that left large empty space on wide viewports — changed both to `width: 100%`. Inputs/textareas were initially left capped at a readable `max-width` inside the now-wider cards; further live feedback on both pages ("why not use 100%") asked for full-width fields too, so that cap was removed as well — every field in both forms now stretches edge-to-edge. **Also fixed, second round of live feedback**: the HTML body textarea was too short (`min-height` 260px → 460px) and had no copy/format affordance — added a small dependency-free HTML re-indenter (`formatHtml`, void/self-closing elements don't nest, everything else does) behind a **Format** button, and a **Copy** button (`navigator.clipboard.writeText`), both above the HTML body field. `ruff`/`mypy`/`alembic check` clean; `apps/api` `pytest` (targeted: `test_templates.py` + `test_protected_routes_audit.py`, not the full suite, to avoid `GRX-EMAIL-011`'s DB-wipe quirk) 11 passed. `eslint`/`tsc --noEmit`/`prettier --check`/`next build` clean; `vitest` 79 passed across both test files (list: access-denied, view-only read+search+preview, manage-links-point-at-dedicated-routes, empty state, search filter, delete confirm/decline/blocked-with-detail; form page: access-denied, create+navigate, live preview while typing, duplicate-prefill, edit-loads-and-saves-new-version, format HTML, copy to clipboard, cancel-navigates-back). Live-verified against Compose end-to-end, using **ref-based clicks after raw screenshot-coordinate clicks intermittently hit the wrong list row once the page reflowed** (a recurrence of an earlier-session lesson): created a real template via the dedicated create page (typed HTML, watched the live preview update, submitted, landed back on the list); opened Edit on it — name field correctly disabled and pre-filled, subject/body pre-filled; opened Duplicate on a concurrent session's own real "Growixa Product Launch" template (rich HTML/CSS) non-destructively (pre-filled form + correct live preview of its real content, then cancelled — no actual duplicate created) without disturbing it; deleted the disposable template via direct API calls (the browser automation environment auto-dismisses native `confirm()` dialogs, so the accept-path was verified via `curl` — 204, gone from `GET /templates` — while the decline-path, no DELETE request sent, was confirmed directly in the browser). Commit `e2a7529` |
| GRX-EMAIL-009 | Email | Campaign builder + send frontend | Draft creation/editing, targeting, test send, immediate send | P1 | DONE | GRX-EMAIL-003 (DONE), GRX-EMAIL-004 (DONE), GRX-EMAIL-007 (DONE), GRX-EMAIL-008 (DONE) | coding agent | `apps/web/src/app/(dashboard)/dashboard/campaigns/` (new: `campaigns-page.tsx`, `campaign-form-page.tsx`, `new/page.tsx`, `[id]/page.tsx`, `types.ts`), `sidebar.tsx`, `page-title.tsx`, `apps/web/src/lib/format-html.ts` (new, extracted from `templates/template-form-page.tsx`) | A `campaigns.manage` user can build a draft; only a `campaigns.send` user sees/can use the send controls | Frontend component tests across `campaigns-page.test.tsx` and `campaign-form-page.test.tsx` | — | 2026-08-06 | 2026-08-06 | No backend changes — every endpoint this task needed (`POST/GET/PATCH /campaigns`, `POST /campaigns/{id}/test-send`, `POST /campaigns/{id}/send`, `GET /integrations/sender-identities`, `GET /contacts/lists`, `GET /contacts/segments`, `GET /templates`) already existed from `GRX-EMAIL-003`/`004`/`006`/`007`/`008`. One `CampaignFormPage` client component, parameterized by `mode: "create" | "edit"`, serves creation, draft-editing, read-only viewing of a non-draft campaign, and the test-send/send-now panel all in one page — `/dashboard/campaigns/new` and `/dashboard/campaigns/[id]` (the `[id]` route also serves as the read/detail view, there's no separate `/edit`). `editable = canManage && (mode === "create" || campaign.status === "DRAFT")` — fields render as disabled inputs (not hidden) once a campaign leaves `DRAFT`, so content stays reviewable; a hint explains why saving isn't available, mirroring the backend's own 409 message. Recipient targeting is one `recipient_type` select (`ALL_CONTACTS`/`LIST`/`SEGMENT`) plus a conditional second select for the list/segment itself, switching type clears the previous target id client-side (mirrors `campaigns/services.py`'s own clearing logic). An optional "Load content from a template" select reads the already-fetched `GET /templates` response (which nests `current_version.{subject,body_html,body_text}`, so no extra per-selection fetch) and prefills the form, keeping `template_id` for lineage — same client-side-prefill shape as `GRX-EMAIL-008`'s Duplicate. Test-send (`campaigns.send`) posts to `/campaigns/{id}/test-send` and is available regardless of campaign status, matching `send_test_email`'s own lack of a status guard; Send now is `DRAFT`-only (backend 409s otherwise) and gated behind `window.confirm`, then optimistically flips local status to `SENDING` and shows a toast rather than polling for the real terminal state. `formatHtml` (the dependency-free HTML re-indenter added ad hoc during `GRX-EMAIL-008`) and its Format/Copy buttons were extracted from `templates/template-form-page.tsx` into `apps/web/src/lib/format-html.ts` and reused here, rather than duplicating the ~35-line function a second time. **Mid-task design change, directly from a user-supplied reference screenshot**: the list page shipped first as row-based (matching `GRX-EMAIL-008`'s templates list), then was rebuilt as a card grid with status-filter tabs after the user shared a reference image — scoped down via `AskUserQuestion` on the two parts the reference implied but this app can't back yet: open/click-rate per card (deferred to `GRX-EMAIL-010`, the dedicated report task, rather than pulling its scope forward with N+1 report fetches) and Scheduled/Paused filter tabs (deferred since scheduled sending isn't built — `campaigns.status`'s CHECK constraint only allows `DRAFT`/`SENDING`/`SENT`/`FAILED` today, matching the still-`BACKLOG` `GRX-SCHED-*` rows elsewhere in this table). `eslint`/`tsc --noEmit`/`prettier --check`/`next build` clean; `vitest` 97 passed (18 new: 8 in `campaigns-page.test.tsx` covering access, manage-gating, status/recipient-target rendering, status-tab filtering incl. asserting no Scheduled/Paused tabs, and search; 10 in `campaign-form-page.test.tsx` covering create-and-navigate-to-detail, template-content-loading, conditional-target-required, draft-is-editable, sent-is-read-only-with-no-Send-now-but-test-send-still-available, send-panel-hidden-without-`campaigns.send`, test-send, send-now-with-confirm, send-now-declined). Live-verified against Compose end-to-end as a real Super Admin: configured a real Custom SMTP connection and sender identity through the Integrations UI (fake host — the point was exercising real credential storage/session flow, not a real send), created a campaign loading real content from an existing template, confirmed the live preview and sender/recipient selects all populated from real API data; test-send correctly round-tripped a real 502 with the underlying DNS-resolution error text; Send now (via a `window.confirm` stub, since the browser-automation environment auto-dismisses native `confirm()` the same way `GRX-EMAIL-008`'s delete flow hit) flipped the UI to `SENDING` immediately and — confirmed on reload — the worker's real job pipeline carried it through to `SENT` (0 recipients, since no contacts exist in this environment, so the send trivially completed with nothing to iterate), proving the full create→send→status-refresh path against the real backend, not just the optimistic client-side update. Cleaned up all smoke-test rows afterward (campaign, sender identity, connection, throwaway Super Admin user) via direct SQL where no delete endpoint exists (campaigns has no `DELETE` route). Commit `09ef796` |
| GRX-EMAIL-010 | Email | Campaign report frontend | Per-campaign delivery/analytics report view | P2 | DONE | GRX-EMAIL-006 (DONE), GRX-EMAIL-009 (DONE) | coding agent | `apps/web/src/app/(dashboard)/dashboard/campaigns/{campaign-form-page.tsx,campaign-form-page.module.css,types.ts}` (extended) | A `campaigns.view` user can see a campaign's delivery report | Frontend component tests extending `campaign-form-page.test.tsx` | — | 2026-08-06 | 2026-08-06 | No backend changes — `GET /campaigns/{id}/report` (`GRX-EMAIL-006`) already returned everything needed (`sent`/`delivered`/`opened`/`clicked`/`bounced`/`complained` raw counts). Added a "Delivery report" card to `CampaignFormPage`'s side column, fetched once the campaign itself loads, only when `status !== "DRAFT"` (a draft has nothing to report yet — no point fetching a guaranteed-all-zero response). The report fetch is wrapped in its own try/catch, separate from the campaign-load try/catch that gates the rest of the page — a report-fetch failure degrades to "no report section" rather than the whole page's `loadError` state, since the report is supplementary to a page whose core content (the campaign itself) already loaded successfully. `formatMetric(count, denominator)` renders each metric as `"{count} ({rate}%)"`, or the bare count when the denominator is 0 (no misleading `0%`/`NaN%`) — `—`-for-empty is this codebase's own established convention (`contacts-page.tsx`), applied here as "just the count" since a raw 0 is already the honest empty value. Sent is a plain count (nothing to divide it by); Delivered/Bounced/Complained are rates of `sent`; Opened/Clicked are rates of `delivered` (can't open or click what wasn't delivered) — matches standard email-marketing rate conventions. `eslint`/`tsc --noEmit`/`prettier --check`/`next build` clean; `vitest` 101 passed (4 new: delivery report renders with correct counts+rates for a `SENT` campaign, report absent for `DRAFT`, zero-denominator shows bare counts not `NaN%`/`0%`, page still renders correctly when the report fetch itself fails). **Evidence gap**: the browser-automation tool used for live verification throughout this session became fully unavailable partway through this task (`navigate` itself denied, not just racing the user's own tab as in earlier tasks) — no live screenshot/click-through was possible this time. Confidence instead rests on: the component test suite's exact percentage-math assertions against the real `formatMetric` implementation (not a mock), a clean `next build`, and the fact that this feature reuses the exact same fetch/permission/render scaffolding from `GRX-EMAIL-009` that was already live-verified end-to-end against the real backend earlier in this same session. Sprint 3 (Email Marketing) is now fully `DONE` — this was its last remaining task. Commit `20dba50` |
| GRX-EMAIL-011 | Email | Custom SMTP as a second provider + SMTP TLS fixes | Per-provider active connections (`email_provider_connections` now allows one active row per provider, DB-enforced); Postmark + Custom SMTP can be connected simultaneously; fixed two real SMTP transport bugs found via live testing | P1 | DONE | GRX-EMAIL-007 (DONE) | coding agent | `apps/api/src/growixa_api/integrations/` (extended), `apps/api/src/growixa_api/email_delivery/{smtp_sender.py,services.py}` (extended), `apps/worker/src/growixa_worker/email_sender.py` (extended), migration `04cce299c2d1`, `apps/web/src/app/(dashboard)/dashboard/integrations/` (rebuilt as a per-provider card grid) | Postmark and Custom SMTP connections can both be active at once, each independently manageable with its own scoped sender identities; sending correctly negotiates implicit TLS (port 465) vs STARTTLS (other ports); SMTP/TLS transport failures surface as a clean error, never an unhandled 500 | Backend integration tests (multi-provider independence, invalid provider 422) + new `smtp_sender`/`email_sender` unit tests (TLS mode selection, error wrapping) + frontend component tests (per-provider grid) | — | 2026-08-05 | 2026-08-06 | Direct user request following a design-reference screenshot (Postmark/SendGrid/Resend/SES/Custom SMTP/Mailgun bento grid) — scoped down via two explicit `AskUserQuestion` rounds to "actually build multi-provider" + "Postmark and Custom SMTP only," logged as [DEC-GRX-016](DECISIONS.md). Migration `04cce299c2d1` expands the provider CHECK to `('POSTMARK', 'CUSTOM_SMTP')` and replaces the old non-unique `is_active` index with a **partial unique index** `(provider) WHERE is_active` — moving "at most one active connection per provider" from an app-only convention to a real DB constraint. `get_active_email_provider_connection`/`deactivate_active_email_provider_connections` are now provider-scoped; `GET /integrations/email-providers` (plural, new) lists all connections, replacing the old singular GET. Frontend rebuilt as a 2-card grid (`PROVIDER_REGISTRY`), each card independently showing connection status, identity count, and a scoped identity list/add form. **Real bug found during backend work**: `test_campaigns.py::test_unauthenticated_requests_are_rejected` requested the `sender_identity_id` fixture but never cleaned it up — harmless before this task's new unique constraint, a genuine failure after; fixed with try/finally. `apps/api` `ruff`/`mypy`/`alembic check` clean; ran full suite twice for stability. **Two further real bugs found live-testing against the user's own real mail server** (`mail.iitdeveloper.com`, sent by the user with their own real SMTP password — never typed by the agent, per this session's credential-handling policy): (1) `smtp_sender.py`/`email_sender.py` hardcoded `start_tls=True` (STARTTLS) unconditionally; port 465 is implicit TLS and just hung waiting for a plaintext banner that never arrives — fixed by selecting `use_tls` vs `start_tls` based on `smtp_port == 465`. (2) once the TLS mode was fixed, a real test-send against the user's server surfaced `ssl.SSLCertVerificationError: certificate has expired` as an **unhandled 500**, not the endpoint's intended 502 "Test send failed" — because `ssl.SSLCertVerificationError` is an `OSError`, not an `aiosmtplib.SMTPException`, so it fell outside `EmailSendError`'s except clause despite its own docstring promising to cover "connection, TLS, auth" failures; fixed by also catching `OSError`. Confirmed the fix end-to-end with diagnostic probes (bypassing cert verification only in a throwaway script, never in shipped code): with the correct TLS mode, the connection reached a real `535 Incorrect authentication data` from the user's server, proving transport+auth both work correctly. The user's actual certificate being expired is an external blocker on their mail host, not this codebase — real sends through that specific server will keep failing until they renew it. Added `test_smtp_sender.py`/`test_email_sender.py` (2 tests each: TLS mode selection, OSError wrapping). Final counts: `apps/api` `pytest` 152 passed, 3 skipped; `apps/worker` `pytest` 12 passed. `ruff`/`mypy`/`prettier`/`eslint`/`tsc --noEmit` clean on both apps; `vitest` 60 passed; `next build` clean. Live-verified against Compose, in two passes either side of the DB resets described below: before the first reset, screenshots confirmed both cards independently reaching Connected/Unconfigured with correctly scoped identities (adding one under Postmark left Custom SMTP's list empty); after the TLS fixes, a real campaign test-send against the user's own server (created via curl, not the UI) returned the expected clean 502 with the certificate-expiry message, replacing the earlier raw 500 — confirmed via the API directly rather than a fresh screenshot. **Known-quirk collision**: running the full `apps/api` suite wipes the dev DB via `test_migrations.py`'s alembic round-trip, which twice during this task also wiped the user's own real Custom SMTP connection they'd just entered, requiring them to re-enter it — recreated `admin@growixa.local` both times; see the new note in `AGENT_HANDOFF.md`. Cleaned up all agent-created smoke-test connection/identity/campaign rows, leaving the user's real connection and identity untouched. Commit `447001c` |
| GRX-EMAIL-012 | Email | "Test connection" button for SMTP provider setup | Validate SMTP host/port/credentials (connect + auth, no message sent) before saving a connection; moved smtp_sender.py into integrations per MODULE_BOUNDARIES.md | P2 | DONE | GRX-EMAIL-011 (DONE) | coding agent | `apps/api/src/growixa_api/integrations/{smtp_transport.py (moved from email_delivery/smtp_sender.py), schemas.py, services.py, api.py}`, `apps/api/src/growixa_api/email_delivery/{services.py,api.py}` (import updates only), `apps/web/src/app/(dashboard)/dashboard/integrations/integrations-page.tsx` | A "Test connection" button in the connection form attempts a real SMTP connect+login using the current (unsaved) form values and shows the real success/failure result, without persisting anything or sending mail | New `smtp_transport` unit tests (TLS kwargs + login, auth-failure wrapping) + frontend component tests (success/failure toast) | — | 2026-08-06 | 2026-08-06 | Direct user follow-up request from `GRX-EMAIL-011`'s live testing ("can we add a test button while configuring"). **Architecture fix required first**: `email_delivery/smtp_sender.py` couldn't be reused from `integrations/services.py` — [MODULE_BOUNDARIES.md](../04-architecture/MODULE_BOUNDARIES.md) only allows `email_delivery` to depend on `integrations`, not the reverse, and a "test connection" endpoint belongs under `/integrations` (connection setup, not delivery). Moved the file to `integrations/smtp_transport.py` (`git mv`, both the module and its test file), updated `email_delivery/services.py` and `email_delivery/api.py`'s imports to the new location — the correct fix per the already-documented dependency direction, not a workaround. Added `test_connection()` to `smtp_transport.py`: opens `aiosmtplib.SMTP(...)` and calls `.login()` only — no message is constructed or sent — reusing the same `_tls_kwargs()` port-465-vs-STARTTLS helper `send_email()` uses (factored out during this same edit to avoid duplicating GRX-EMAIL-011's TLS fix in two places), with a short 10s timeout instead of `send_email`'s implicit 60s default since this is an interactive, UI-blocking check. New `EmailProviderConnectionTestIn` schema (no `provider` field — connecting doesn't depend on which provider these credentials belong to) and `POST /integrations/email-providers/test` (204 on success, 502 with the real underlying error message on failure, gated by the existing `integrations.manage` permission — no new permission needed). Nothing is persisted by this endpoint; it exists purely to validate before (or instead of) saving. Frontend: new "Test connection" button in the connection form, disabled until all four fields are filled, showing a real backend error message in the toast (a deliberate exception to this codebase's usual canned-message convention — the whole point of this feature is surfacing *which* SMTP failure occurred). `ruff`/`mypy`/`alembic check` clean; `apps/api` targeted tests (`test_smtp_transport.py`, `test_integrations.py`, `test_email_delivery.py`, `test_campaigns.py`, `test_protected_routes_audit.py` — deliberately not the full suite, to avoid re-triggering the DB-wipe quirk `GRX-EMAIL-011` just hit) all pass, 40 passed. `eslint`/`tsc --noEmit`/`prettier --check`/`next build` clean; `vitest` 62 passed (2 new: success toast, failure toast shows the parsed backend detail). **Found and fixed a test-only issue while adding the frontend tests**: the existing `vi.mock("@/lib/api-client", () => ({ apiFetch: vi.fn() }))` fully replaced the module, so the component's own new `import { ApiError }` resolved to `undefined` under test (Vitest throws a distinct "use importOriginal" error on access rather than silently returning undefined) — switched to `vi.mock(path, async (importOriginal) => ({ ...(await importOriginal()), apiFetch: vi.fn() }))` to keep real exports like `ApiError` while still mocking `apiFetch`. Live-verified against Compose: restarted the `api` container (bind-mounted source, no image rebuild needed) and the `web` container (dev-server file-watcher quirk noted in `GRX-EMAIL-007`'s entry recurred for this new button); in the browser, opened Postmark's configure form, entered bad credentials, clicked "Test connection" — button showed "Testing…", then a toast reading the real Postmark response `Connection test failed: (535, '5.7.8 Error: authentication failed: UGFzc3dvcmQ6')`; confirmed via network inspection that the request/response matched exactly. No success-path live verification was possible (no valid Postmark credentials in this environment, and the user's own Custom SMTP server's certificate is still expired per `GRX-EMAIL-011`) — covered instead by the unit and component tests. This endpoint persists nothing, so no smoke-test cleanup was needed. Commit `aa83974` |
| GRX-SCHED-001 | Scheduled Campaigns | Scheduled campaigns database schema & migration | Extend `campaigns` table with `scheduled_at`, `status` (`DRAFT`, `SCHEDULED`, `DISPATCHING`, `SENT`, `CANCELLED`, `FAILED`), `idempotency_key`, and `cancelled_at` | P0 | DONE | GRX-EMAIL-004 (DONE) | coding agent (two sessions) | `apps/api/src/growixa_api/campaigns/{models.py,schemas.py,services.py,api.py}`, migrations `4a92b8107c12` and `caf1c42204c9`, `apps/api/tests/test_campaigns_scheduler.py` | Migration creates index on `(status, scheduled_at)` and adds status constraints | Migration test + `test_campaigns_scheduler.py` | — | 2026-08-06 | 2026-08-06 | Per the plan in `docs/14-sprints/SPRINT_04_SCHEDULED_CAMPAIGN.md`. **Built independently and in parallel by two sessions working from that same plan doc** — this row's own detail merges both: a concurrent session merged `4a92b8107c12` directly to `main` (schema + `POST /campaigns/{id}/schedule` + `POST /campaigns/{id}/cancel` + `schedule_campaign`/`cancel_campaign` services + mock-based tests), landing ahead of and superseding this session's own from-scratch version of the same migration (whose in-progress uncommitted state — plus an uncommitted tracker edit — was silently discarded when that merge ran, since neither had been committed yet; no real work was lost, since the two versions covered the same ground). Two real bugs found and fixed on top of the merged version, both confirmed via `alembic check`/`alembic heads`, not assumed: (1) the merged migration's `down_revision` pointed to `f5ecaa79863b` instead of the actual prior head `04cce299c2d1` (GRX-EMAIL-011's Custom SMTP migration) — a real broken migration graph (two divergent heads; `alembic upgrade head` failed outright; a fresh database would have silently skipped GRX-EMAIL-011's schema entirely) — already fixed by the other session by the time this was checked again. (2) After that fix, `alembic check` still reported drift: this dev database had already applied an *earlier draft* of `4a92b8107c12` (from before the merge, part of this session's own now-discarded work) that defined `idempotency_key` as a nullable `TEXT` column, while the merged file's current version defines it as `UUID NOT NULL UNIQUE` — Alembic's bookkeeping only tracks revision IDs, not file content, so it kept reporting the revision as fully applied despite the actual column no longer matching. Fixed with a new corrective migration `caf1c42204c9` (`campaigns` has zero rows in every environment this touched, so no data migration needed — just drop-and-recreate to match `4a92b8107c12`'s own definition) rather than editing already-applied history. `alembic check` clean after; downgrade→upgrade round trip verified for both migrations; `pytest tests/test_campaigns.py tests/test_campaigns_scheduler.py tests/test_protected_routes_audit.py` (targeted, not the full suite, to avoid the known DB-wipe quirk — a throwaway Super Admin user/connection/identity from `GRX-EMAIL-009`/`010` live verification were still sitting in this dev DB) — 19 passed. **Still open, not part of this row**: no scheduler ticker (`GRX-SCHED-002`) or worker dispatch consumer (`GRX-SCHED-003`) exist yet — a campaign can be marked `SCHEDULED` via the API now, but nothing automatically dispatches it at the scheduled time. `test_campaigns_scheduler.py` uses `unittest.mock`/`AsyncMock` rather than this codebase's established real-Postgres integration-test convention (`user_factory`/real DB sessions via `conftest.py`) — noted, not changed, since it's the other session's test file. Commit `2ab4784` |
| GRX-SCHED-002/003/004/005/006 | Scheduled Campaigns | Scheduler ticker, worker dispatch consumer, idempotency/retry, DLQ, tests | Poll `campaigns` for due `SCHEDULED` rows and dispatch them via a RabbitMQ worker with Redis idempotency and exponential-backoff retry | P0 | DONE | GRX-SCHED-001 (DONE) | coding agent | `apps/api/src/growixa_api/campaigns/scheduler.py` (new), `apps/api/src/growixa_api/app.py` (lifespan), `apps/api/src/growixa_api/config.py`, `apps/api/tests/test_campaign_scheduler_ticker.py` (new), `apps/api/migrations/versions/caf1c42204c9_*.py` (downgrade fix), `apps/worker/src/growixa_worker/{consumer.py,redis_client.py (new),config.py,models.py}`, `apps/worker/tests/test_dispatch_consumer.py` (new), `apps/worker/pyproject.toml`, `compose.yaml` | A `SCHEDULED` campaign past its `scheduled_at` is atomically claimed, dispatched by the worker, retried up to 3x with 1m/5m/15m backoff on transient failure, and lands in `grx.campaigns.dlq` + `FAILED` after exhausting retries | `test_campaign_scheduler_ticker.py` (3), `test_dispatch_consumer.py` (4), full `pytest`/`ruff`/`mypy`/`alembic check` both apps | — | 2026-08-06 | 2026-08-06 | Per the user's explicit instruction to build the rest of Sprint 4 rather than just document the gap. **Scheduler (`GRX-SCHED-002`)**: `claim_due_campaigns()` does a single `UPDATE campaigns SET status='DISPATCHING' WHERE status='SCHEDULED' AND scheduled_at <= NOW() RETURNING *` (safe under concurrent tickers via Postgres row locks, unlike SELECT-then-UPDATE), then `run_scheduler_tick()` commits and publishes one `grx.campaigns.dispatch` job per claimed campaign; `run_scheduler_loop()` runs forever as a `FastAPI` `lifespan` background `asyncio` task (confirmed via `inspect.signature` that `httpx.ASGITransport` never drives the lifespan protocol, so no existing test accidentally starts it). Placed in `campaigns/scheduler.py`, not `growixa_api.jobs` as the sprint doc sketched, since it's campaign-specific business logic co-located with the rest of that domain, not generic job plumbing. **Worker dispatcher (`GRX-SCHED-003`)**: `make_dispatch_handler` reuses `handle_send_campaign` unmodified — confirmed by reading it that it's status-agnostic and already idempotent via a `CampaignVersion`-existence check, so it's safely shared between the immediate "Send now" path and scheduled dispatch. **Idempotency & retry (`GRX-SCHED-004`)**: Redis marks a job's `idempotency_key` done *after* success, not before attempting, since claim-before-attempt would incorrectly block a legitimate retry after a transient failure — the DB-level `CampaignVersion` check remains the authoritative guard either way. Retries use the RabbitMQ TTL+dead-letter-exchange delay pattern (three durable wait queues with `x-message-ttl` 60000/300000/900000ms and `x-dead-letter-routing-key` back to `grx.campaigns.dispatch` — no delay plugin needed) rather than a client-side sleep, so retries survive a worker restart. **DLQ (`GRX-SCHED-005`'s DLQ half — the cancel-endpoint half already shipped under `GRX-SCHED-001`)**: after `MAX_DISPATCH_ATTEMPTS` (3) the job is republished to `grx.campaigns.dlq` and the campaign is marked `FAILED`. **Tests (`GRX-SCHED-006`)**: 3 ticker tests (claim-scoping, one-job-per-claim, no-op-when-nothing-due) against real Postgres; 4 dispatch-consumer tests (idempotency skip, mark-done-after-success, retry-queue routing, DLQ+FAILED-after-max-attempts) against real Postgres with a fake in-memory Redis/AMQP-channel stand-in, since what's under test is consumer.py's own routing decisions, not aio_pika's/redis-py's wire behavior. **Two real bugs found and fixed along the way, neither assumed**: (1) `caf1c42204c9`'s `downgrade()` and `4a92b8107c12`'s `downgrade()` both dropped a unique constraint of the same name (`uq_campaigns_idempotency_key`) — harmless on the way up (redundant no-op) but fatal on the way down (`test_alembic_upgrade_head_then_downgrade_base_round_trips_cleanly` failed with "constraint does not exist" on the second drop); root-caused by testing single-step vs. multi-step downgrades directly via `alembic.command`, not by guessing — fixed by making `caf1c42204c9`'s upgrade/downgrade both no-ops, since its only job was already fully subsumed by `4a92b8107c12`'s own (correct) definition; it stays in the revision chain purely as a historical marker. (2) The live `growixa-api-1`/Compose container had **zero volume mounts** (`docker inspect` showed `Mounts: []`) despite `compose.yaml` defining bind mounts for `src`/`migrations` — it had been created before those mounts existed in `compose.yaml` and `docker compose restart` doesn't reapply config (only recreation does), so it had been silently serving a stale baked-in image this entire session; `docker compose up -d --build api` recreated it correctly. Caught while investigating why the scheduler's own log line never appeared live — which led to a third, smaller fix: nothing in `growixa_api` ever called `logging.basicConfig`, so `logging.getLogger("growixa_api")` had no handler and every `logger.info`/`.exception` call in `scheduler.py` was silently going nowhere (uvicorn only configures its own `uvicorn.*` loggers); added a `logging.basicConfig` call to `create_app()`. Live-verified against Compose after both fixes: the scheduler's startup log line appears, RabbitMQ (`rabbitmqctl list_queues`) shows all 5 queues with the correct TTL/DLX arguments, and multiple ticks ran with zero errors. `ruff`/`ruff format`/`mypy` clean on every file this row touched, both apps; `alembic check` clean. Full-suite `pytest` was **not** used as the final gate — this dev DB is being actively used concurrently by another live session/user throughout this work (real HTTP traffic in the API logs, a live SMTP-cert-expiry error from the user, and an in-progress uncommitted edit to `test_email_delivery.py` from another session fixing the same unscoped-`DELETE`-in-cleanup pattern found here); pre-existing, unrelated integration tests (`test_integrations.py`, `test_campaigns.py`) flaked only when run as part of the full suite and passed cleanly in isolation, consistent with collision against that concurrent traffic rather than a regression — and `test_send_campaign.py`'s own pre-existing unscoped `_cleanup()` incidentally wiped ~900 rows of what looked like disposable CSV-import sample data (`sample_1000_contacts.csv`, still on disk) while running its existing, unmodified suite. Every file this row actually touches was re-verified individually and passes reliably. Commit `14fb258` |
| GRX-SCHED-FRONTEND | Scheduled Campaigns | Scheduled campaigns UI (Send Mode toggle, datetime picker, Cancel button, status badges, Scheduled filter tab) | Complete campaign scheduling lifecycle on frontend (Send Now vs Schedule for Later, datetime picker, Cancel button on draft/scheduled rows, status badges & Scheduled filter tab) | P0 | DONE | GRX-SCHED-001 (DONE), GRX-SCHED-002 (DONE) | coding agent | `apps/web/src/app/(dashboard)/dashboard/campaigns/campaign-form-page.tsx`, `campaign-form-page.module.css`, `campaigns-page.tsx`, `campaigns-page.module.css`, `types.ts`, `campaign-form-page.test.tsx`, `campaigns-page.test.tsx` | Schedule for Later radio group + datetime-local input (min = now+10m) converts to UTC ISO and posts to `/campaigns/{id}/schedule`; Cancel button (DRAFT/SCHEDULED only, canManage gate) posts to `/campaigns/{id}/cancel`; status badges for 7 statuses; Scheduled tab in FILTER_TABS | `vitest` 110 passed (9 new), `tsc --noEmit` 0 errors | — | 2026-08-06 | 2026-08-06 | Built in worktree `.worktrees/grx-sched-frontend` on branch `feature/FRONTEND/GRX-SCHED-UI` per `docs/14-sprints/SPRINT_04_SCHEDULED_CAMPAIGN_FRONTEND.md`. Widened `CampaignStatus` type to include `SCHEDULED`, `DISPATCHING`, `CANCELLED`; added `scheduled_at`, `cancelled_at`, `idempotency_key` fields. Gated Send Mode radio group and Cancel button behind `canManage` permission (matching backend `_require_manage` check). All 110 web unit tests pass; `tsc --noEmit` clean; Prettier and ESLint clean. Merged cleanly to `main` (commit `e6335e7`) and verified end-to-end against live API & background ticker. Worktree cleaned up after merge. Commit `e6335e7` |
| GRX-WEB-001 | Web Architecture | Next.js App Router Groups refactoring | Restructure `apps/web/src/app/` into `(marketing)`, `(auth)`, `(dashboard)`, and `(admin)` route groups with shared group layouts | P0 | DONE | GRX-FOUND-004 (DONE) | coding agent | `apps/web/src/app/` (`(marketing)`, `(auth)`, `(dashboard)`, `(admin)`) | Existing `/login`, `/dashboard`, `/` URLs operate seamlessly; `npm run typecheck`, `vitest`, and `playwright e2e` tests pass clean | Typecheck & e2e smoke tests | — | 2026-08-04 | 2026-08-04 | Refactored `apps/web/src/app/` into `(marketing)`, `(auth)`, `(dashboard)`, and `(admin)` route groups. Added `(marketing)/layout.tsx`, `(auth)/layout.tsx`, `(admin)/layout.tsx`. `tsc --noEmit`, `vitest` (54 passed), and Playwright e2e (4 passed) clean. Commit `e0b018c` |
| GRX-WEB-002 | Brand Website | Public 3D Brand & Landing Website | Build 3D Landing Page (`/`), `/features`, `/pricing`, `/solutions`, `/security`, `/docs` using Linear/Vercel/Stripe aesthetics, Inter/Geist font stack, and 3D preview cards | P1 | DONE | GRX-WEB-001 (DONE) | coding agent | `apps/web/src/app/(marketing)/` | All public brand pages load clean, responsive, and pass frontend component tests | Frontend component tests | — | 2026-08-04 | 2026-08-04 | Built public 3D Brand & Landing Website at `apps/web/src/app/(marketing)/`. Built Navbar (`BrandLogo`), HeroSection with 3D Dashboard preview, AiTeamSection, WorkflowShowcase, IntegrationsSection, SecuritySection, PricingSection, Footer, and subpages (`/features`, `/pricing`, `/solutions`, `/security`, `/docs`). `tsc --noEmit`, `vitest` (54 passed), and Playwright e2e (4 passed) clean. Commit `447001c` |
| GRX-ADMIN-001 | Admin Portal | System Platform Control Plane & Health Panel (`(admin)`) | Build `/admin` surface for platform health metrics, authenticated layout gate, and admin navigation | P1 | DONE | GRX-WEB-001 (DONE), GRX-EMAIL-001 (DONE) | coding agent | `apps/web/src/app/(admin)/layout.tsx`, `admin-header.tsx`, `admin-header.module.css`, `admin/page.tsx`, `admin/admin-page.module.css`, `admin/admin-page.test.tsx`, `(dashboard)/dashboard/sidebar.tsx` | Admin control plane renders live infrastructure status (Postgres, Redis, RabbitMQ) and triggers ops healthcheck jobs for Super Admin / Admin role | `vitest` 120 passed (6 new in `admin-page.test.tsx`), `tsc --noEmit` 0 errors | — | 2026-08-06 | 2026-08-06 | Built in worktree `.worktrees/grx-admin-health-panel` on branch `feature/FRONTEND/GRX-ADMIN-001-HEALTH-PANEL` per `docs/14-sprints/SPRINT_ADMIN_001_HEALTH_PANEL.md`. System providers, global user management, and audit logs deliberately excluded from relocation into `(admin)` as they already exist as Super-Admin/Admin-gated pages under `/dashboard` (`/dashboard/integrations`, `/dashboard/team`, `/dashboard/audit`). Added real auth gate to `(admin)/layout.tsx` (redirects unauthenticated users to `/login` and users without `admin.access` to `/dashboard`). Created `/admin` System Health page with live status pills (Postgres/Redis/RabbitMQ from `GET /health`), raw error detail display for failing checks, top-level operational/degraded banner, manual Refresh button, and "Run healthcheck job" button triggering `POST /system/jobs/healthcheck` with toast notification of returned `job_id`. Added `System Health` nav link (gated by `admin.access`) to sidebar SETTINGS section. Merged cleanly to `main` (commit `c93af62`). Commit `c93af62` |
| GRX-SAAS-001 | Multi-Tenant Platform | Phase A — Multi-tenant data isolation | New `accounts` table; `account_id` FK added to every account-owned table (~30 tables across users/auth, company, contacts, email — see `SPRINT_05_MULTI_TENANT_PLATFORM.md` Phase A for the full grouped list); every repository query scoped by `account_id`; new `get_current_account_id()` dependency; existing single-tenant data backfilled into one seeded account; cross-tenant isolation test suite for every module | P0 | READY | GRX-EMAIL-011 (DONE) | unassigned | Every `apps/api/src/growixa_api/*/models.py`, `repositories.py`, `services.py`; new Alembic migration; new `permissions/dependencies.py`-style `get_current_account_id()`; `apps/api/tests/conftest.py` (`account_factory`) | Every account-owned table has a `NOT NULL account_id` FK + index; every existing integration test passes with an explicit account fixture; a new cross-tenant isolation test exists per module and fails if a scoping filter is removed; `alembic check` clean, migration reversible | Cross-tenant isolation tests (new category, one per module) + full existing suite re-passing | — | — | — | — |
| GRX-SAAS-002 | Multi-Tenant Platform | Phase B — Platform auth boundary | New `platform_admins`/`platform_permissions`/`platform_role_permissions` tables (no `account_id` — not scoped to any account); new `require_platform_permission(code)` dependency, structurally separate from `require_permission`; separate `POST /platform/auth/login` route + separate session cookie name; new `apps/web/src/app/(platform)/` route group (distinct from today's `(admin)`) | P0 | BACKLOG | GRX-SAAS-001 | unassigned | `apps/api/src/growixa_api/platform_auth/` (new), `apps/web/src/app/(platform)/` (new) | A platform-admin session cookie is never accepted by any `/auth/*` route and vice versa (401 both directions, tested); `require_permission`/`require_platform_permission` are separate functions, verified by a static route-audit test | Integration tests for cross-boundary cookie rejection; route-protection audit test extended to cover `platform.*` | — | — | — | — |
| GRX-SAAS-003 | Multi-Tenant Platform | Phase C — Self-service registration | `POST /accounts/register` + `POST /accounts/verify-email`, reusing the existing token generate/hash pattern from `auth/tokens.py`; plan-selection step (recorded only, not enforced); public signup UI under `(auth)/register`; rate limiting reusing `auth/rate_limit.py` | P1 | BACKLOG | GRX-SAAS-001 | unassigned | `apps/api/src/growixa_api/accounts/` (new), `apps/web/src/app/(auth)/register/` (new) | A new visitor can register, verify email, select a plan, and log in with no existing-account action; an unverified account cannot authenticate; duplicate-email-across-accounts behavior explicitly resolved (default: rejected) before the uniqueness constraint is written | Integration tests: register→verify→login happy path, unverified-login-rejected, duplicate-email-rejected, rate-limit-429 | — | — | — | — |
| GRX-SAAS-004 | Multi-Tenant Platform | Phase D — Billing | Payments vendor integration (Stripe assumed, confirm before starting); `subscriptions` table; authenticated Stripe webhook receiver (signature-verified, mirrors the existing Postmark webhook's "verify before touching any table" shape); plan-limit wiring into the existing `usage_records` table (`DEC-GRX-007`) | P1 | BACKLOG | GRX-SAAS-001, GRX-SAAS-003 | unassigned | `apps/api/src/growixa_api/billing/` (new) | A test-mode subscription can be created/updated/canceled and stays in sync via webhook; a payment failure transitions the account to `PAST_DUE` and is queryable | Integration tests incl. webhook signature rejection, subscription state transitions | — | — | — | — |
| GRX-SAAS-005 | Multi-Tenant Platform | Platform Admin — Account/user management | List all accounts; activate/suspend/close; view login/security activity | P1 | BACKLOG | GRX-SAAS-002 | unassigned | `apps/api/src/growixa_api/platform_admin/` (new), `apps/web/src/app/(platform)/accounts/` (new) | A `platform.admin` can list, suspend, and reactivate any account; a suspended account's users cannot log in | Integration tests + frontend component tests | — | — | — | — |
| GRX-SAAS-006 | Multi-Tenant Platform | Platform Admin — Subscription management | View/change plans, trial extensions, usage credits, reading `GRX-SAAS-004`'s `subscriptions` table | P1 | BACKLOG | GRX-SAAS-002, GRX-SAAS-004 | unassigned | `apps/web/src/app/(platform)/subscriptions/` (new) | A `platform.finance`/`platform.admin` user can view and change any account's plan | Integration tests + frontend component tests | — | — | — | — |
| GRX-SAAS-007 | Multi-Tenant Platform | Platform Admin — Provider management | Platform-level provider config, health, credential rotation — platform-wide analogue of the existing per-account `GRX-EMAIL-011` integrations pattern | P2 | BACKLOG | GRX-SAAS-002 | unassigned | `apps/web/src/app/(platform)/providers/` (new) | A `platform.operations` user can view provider health and rotate platform-level credentials | Integration tests + frontend component tests | — | — | — | — |
| GRX-SAAS-008 | Multi-Tenant Platform | Platform Admin — Usage tracking + campaign oversight | Per-account `usage_records` view across all accounts; queued/failed campaigns across all accounts; abuse controls (pause suspicious sending) | P1 | BACKLOG | GRX-SAAS-002 | unassigned | `apps/web/src/app/(platform)/usage/`, `(platform)/campaigns/` (new) | A `platform.support`/`platform.admin` user can see any account's usage and pause a suspicious campaign | Integration tests + frontend component tests | — | — | — | — |
| GRX-SAAS-009 | Multi-Tenant Platform | Platform Admin — Infra monitoring + financial dashboard | Extends `GRX-ADMIN-001`'s `/health` pattern with RabbitMQ queue-depth introspection (no endpoint exposes this today — confirmed via this project's own live debugging needing raw `rabbitmqctl` calls); MRR/ARR/churn from `GRX-SAAS-004`'s subscription data | P2 | BACKLOG | GRX-SAAS-002, GRX-SAAS-004, GRX-ADMIN-001 (DONE) | unassigned | `apps/api/src/growixa_api/health.py` (extended), `apps/web/src/app/(platform)/monitoring/`, `(platform)/finance/` (new) | Queue depths for `grx.campaigns.dispatch`/retry/DLQ are visible in the platform panel; MRR/ARR/churn compute correctly against seeded test subscription data | Integration tests + frontend component tests | — | — | — | — |
| GRX-SAAS-010 | Multi-Tenant Platform | Platform Admin — Secure support session | Audited, time-limited, banner-visible, read-by-default support access into a customer account — reason + ticket number required, platform-admin confirmation, full audit log, no raw API key exposure, separate permission gate for write access. Deliberately last: highest-trust capability in the sprint | P2 | BACKLOG | GRX-SAAS-002, GRX-SAAS-005 | unassigned | `apps/api/src/growixa_api/support_session/` (new), `apps/web/src/app/(platform)/` + a customer-facing banner component | A support session requires a reason/ticket, expires automatically, is fully audited, shows a visible banner in the customer's UI while active, and cannot read raw API keys; write access requires a separate, explicit permission | Integration tests: session expiry, audit completeness, read-only-by-default enforcement, banner visibility | — | — | — | — |

## Explicitly not tracked here (do not add without a new decision)

Scheduled email (Slice 4), social integrations (Slice 5), AI generation (Slice 6),
SEO/AEO/GEO/crawler work, WordPress/GitHub integration, billing — all out of scope until
their owning slice's planning pass. (Email campaigns — Slice 3 — are now tracked below as
`GRX-EMAIL-*`, as of this update; this note previously listed them as untracked when it
was written for Sprint 1.) See
[SPRINT_01_FOUNDATION.md §Exclusions](../14-sprints/SPRINT_01_FOUNDATION.md#explicitly-excluded-from-sprint-1)
and [SPRINT_02_CONTACTS.md §Exclusions](../14-sprints/SPRINT_02_CONTACTS.md#explicitly-excluded-from-sprint-2).
