# Local Development

- Document ID: DOC-DEVOPS-LOCAL
- Status: ACTIVE
- Version: 1.0
- Last updated: 2026-07-22
- Owner: Coding agent
- Related documents: [SYSTEM_ARCHITECTURE](../04-architecture/SYSTEM_ARCHITECTURE.md), [TEST_STRATEGY](../10-testing/TEST_STRATEGY.md), [MASTER_TASK_TRACKER](../00-project-control/MASTER_TASK_TRACKER.md)

This document previously existed only as an implicit consequence of the Docker Compose task
(`GRX-FOUND-002`). This is now the standalone reference; exact commands are finalized as
each foundation task lands and this file is updated to match — it must never drift from
what actually works.

## Required software

| Tool | Purpose | Notes |
|---|---|---|
| Docker + Docker Compose | Runs Postgres, Redis, RabbitMQ, backend, frontend, worker locally | Required |
| Python 3.13+ | Backend / worker | Match the version pinned in `apps/api/pyproject.toml` |
| Node.js 22+ / npm | Frontend | Match the version pinned in `apps/web/package.json` (`engines`) |
| `uv` or `pip` | Python dependency management | `uv` preferred for speed; `pip` works |
| Git | Version control | — |

## Repository setup

```bash
git clone <repository-url> growixa
cd growixa
```

If you already have the repository locally, just `cd` into it — there is nothing else to
initialize at the VCS level (git is already set up).

## Environment file setup

Each app directory carries its own `.env.example`; copy it to `.env` and fill in local
values before starting anything:

```bash
cp apps/api/.env.example apps/api/.env
cp apps/web/.env.example apps/web/.env.local
```

`.env` and `.env.local` are git-ignored (see root `.gitignore`) and must never be committed.

## Local secrets guidance

- Local `.env` values are for local development only — never reuse a production secret
  locally, and never commit a real secret to a `.env.example` file (those contain
  placeholder values only, e.g. `CHANGE_ME`).
- The Argon2id pepper/JWT signing key used locally must be a locally-generated development
  value, distinct from any future staging/production value.
- If you ever paste a real secret into a terminal command, treat that shell history as
  sensitive — this applies equally to you (the developer) and to any coding agent working
  in this repository per [AGENT_EXECUTION_RULES.md §During development](../12-development/AGENT_EXECUTION_RULES.md#during-development)
  ("protect secrets — never log provider credentials").
- No provider secrets (email/social/AI) exist to configure yet in Sprint 1 — those arrive
  with their owning slice.

## Docker Compose services

| Service | Image/build | Purpose |
|---|---|---|
| `postgres` | `postgres:16` (or pinned minor version) | Primary database |
| `redis` | `redis:7` | Cache, rate limiting, locks |
| `rabbitmq` | `rabbitmq:3-management` | Job queue + management UI |
| `api` | build from `apps/api/` | FastAPI backend |
| `web` | build from `apps/web/` | Next.js frontend |
| `worker` | build from `apps/worker/` | RabbitMQ consumer process (`GRX-FOUND-007`) |

## Service ports (local defaults)

| Service | Port | Notes |
|---|---|---|
| `web` | 3000 | Next.js dev server |
| `api` | 8000 | FastAPI + OpenAPI docs at `/docs` |
| `postgres` | 5432 | |
| `redis` | 6379 | |
| `rabbitmq` | 5672 | AMQP |
| `rabbitmq` management UI | 15672 | Web UI, default guest/guest for local only |

## Startup commands

```bash
docker compose up --build
```

Add `-d` to run detached. First run will be slower while images build and dependencies install.

## Migration commands

```bash
docker compose exec api alembic upgrade head
```

To create a new migration after changing models:

```bash
docker compose exec api alembic revision --autogenerate -m "describe the change"
```

Always review an autogenerated migration before committing it — autogeneration is a
starting point, not a guarantee of correctness.

## Seed / first-admin setup

Sprint 1 ships a one-time seed command that creates the six roles, the Sprint 1 permission
set, and a first Super Admin account from `.env` values (email/password), so there is a way
to log in on a freshly migrated database:

```bash
docker compose exec api python -m growixa_api.cli.seed_first_admin
```

Exact module path is finalized when `GRX-AUTH-001` (users/roles/permissions schema + seed)
is implemented; this file is updated to match at that point.

### Platform admin seed (GRX-SAAS-005/008 follow-up)

`platform_admins` is a structurally separate identity class from customer users
(`DEC-GRX-018`) with no self-service signup, so the first one has to be created
directly. A one-time, idempotent seed command creates a `platform.owner` admin from
`PLATFORM_ADMIN_EMAIL`/`PLATFORM_ADMIN_PASSWORD` (optionally `PLATFORM_ADMIN_FULL_NAME`)
env vars — safe to re-run (e.g. on every deploy), it does nothing if that email already
exists:

```bash
docker compose exec api python -m growixa_api.cli.seed_platform_admin
```

On Render, run the same command from the `api` service's Shell tab after setting those
three env vars in its dashboard. Log into the platform panel at `/platform/login` with
the seeded credentials, then rotate the password once you're in (no self-service
password-change exists yet — use the same env-var-and-rerun approach, or update
`platform_admins.password_hash` directly, until one is built).

### Self-registration verification email (GRX-SAAS-003 follow-up)

`POST /accounts/register` only actually emails the verification link if
`PLATFORM_SMTP_HOST` is set — otherwise it logs a warning and no-ops (registration still
succeeds; `ENVIRONMENT=local`/`test` keep returning the raw token in the response body
for convenience instead). Set on the `api` service:

| Env var | Meaning |
|---|---|
| `PLATFORM_SMTP_HOST` / `PLATFORM_SMTP_PORT` | e.g. an SMTP relay endpoint; port 465 is implicit TLS, anything else is STARTTLS |
| `PLATFORM_SMTP_USERNAME` / `PLATFORM_SMTP_PASSWORD` | Credentials for that relay |
| `PLATFORM_SMTP_FROM_EMAIL` / `PLATFORM_SMTP_FROM_NAME` | The visible sender on the verification email |
| `FRONTEND_BASE_URL` | The deployed frontend's origin (e.g. `https://growixa.netlify.app`) — used to build the `/verify-email?token=...` link |

This is deliberately separate from the customer-owned `email_provider_connections`
table (Postmark/Custom SMTP per account, `GRX-EMAIL-011`) — a brand-new account has no
provider of its own yet at the moment it registers.

### Social Publishing (Instagram) setup (Slice 5, GRX-SOCIAL-011)

Instagram Business publishing ([DEC-GRX-023](../00-project-control/DECISIONS.md)) and
media storage ([DEC-GRX-024](../00-project-control/DECISIONS.md)) both require external
accounts before a customer can connect and publish for real. Without them, the app still
runs: the Integrations page shows Instagram as "Unconfigured", and OAuth
connect/publish routes fail cleanly instead of crashing.

**1. Meta Developer App + test Instagram Business account**

- Create an app at [developers.facebook.com](https://developers.facebook.com/), add the
  **Instagram Graph API** product.
- You need a Facebook Page with an Instagram **Business** (not Personal/Creator-only)
  account linked to it. Meta's Instagram Business login docs walk through this pairing.
- Under App Settings → Basic, note the **App ID** and **App Secret**.
- Under the Instagram Graph API product's settings, add an OAuth redirect URI matching
  `API_PUBLIC_URL` exactly: `http://localhost:8000/integrations/instagram/oauth/callback`
  for local dev (Meta requires HTTPS for non-localhost URIs).
- While the app is in Development mode, only users added as Testers/Developers on the
  app can complete the OAuth consent screen — add your own Meta account.

**2. Supabase Storage project + public bucket**

- Create a free project at [supabase.com](https://supabase.com/).
- Under Storage, create a bucket (default name `social-media`, matching
  `SUPABASE_STORAGE_BUCKET`) and mark it **public** — Instagram's media-container API
  fetches images by plain URL, so the bucket cannot be private (see
  [THREAT_MODEL.md](../08-security/THREAT_MODEL.md) T48 for the accepted-risk reasoning).
- Under Project Settings → API, copy the Project URL (`SUPABASE_STORAGE_URL`) and the
  **service_role** secret key (`SUPABASE_STORAGE_SERVICE_KEY` — never the anon key, the
  API needs write access).

**3. Set the env vars**

Set these in the root `.env` (Docker Compose passes them through to both the `api` and
`worker` services — see `compose.yaml`):

| Env var | Meaning |
|---|---|
| `INSTAGRAM_APP_ID` / `INSTAGRAM_APP_SECRET` | From the Meta Developer App |
| `INSTAGRAM_GRAPH_API_VERSION` | Defaults to `v21.0` |
| `API_PUBLIC_URL` | Must exactly match the redirect URI registered with Meta |
| `SUPABASE_STORAGE_URL` / `SUPABASE_STORAGE_SERVICE_KEY` / `SUPABASE_STORAGE_BUCKET` | From the Supabase project |

Restart the `api` and `worker` containers after changing `.env`
(`podman compose restart api worker` / `docker compose restart api worker`) — Compose
only re-reads `.env` on container start, not on file save.

### AI Assistant setup (Slice 6, GRX-AI-011)

Unlike every other external integration in this project, the AI provider
([DEC-GRX-026](../00-project-control/DECISIONS.md)) is **not** configured via `.env` at
all — it's DB-backed and editable at runtime, on purpose, so a platform admin can change
providers without a redeploy. Without it configured, the app still runs: customers can
use every other feature, but `POST /ai/generate/{capability}` returns a 409
("AI not configured") until a default is set.

**1. Configure the platform-wide default**

Sign in as a platform admin with `platform.ai.manage` (`platform.owner`/`platform.admin`)
and either use the **AI & LLM Config** page in the platform admin panel
(`/platform/ai-config`), or call the API directly:

```bash
curl -X PUT http://localhost:8000/platform/ai-config \
  -H "Content-Type: application/json" \
  --cookie "platform_access_token=<your platform admin session token>" \
  -d '{"provider": "OPENAI", "api_key": "sk-...", "default_model": "gpt-4o-mini"}'
```

`provider` is one of `OPENAI` / `AZURE_OPENAI` / `ANTHROPIC` / `OLLAMA`. `AZURE_OPENAI`
and `OLLAMA` also require `base_url` (Azure's resource endpoint / your Ollama host) —
both platform-admin and account-level `base_url` values are SSRF-validated
([DEC-GRX-027](../00-project-control/DECISIONS.md)): private, loopback, link-local, and
cloud-metadata addresses are rejected. Use the page's (or `POST
/platform/ai-config/test`'s) Test Connection action to verify credentials before saving
— it makes one real, minimal generation call and persists nothing.

**2. Optional: an account brings its own provider instead**

Any account's Super Admin can override the platform default for that account alone, via
the Integrations page's **AI Model Provider** card (or `POST /ai/connections` directly)
— the account's own connection always takes priority over the platform default when
both exist.

No env vars, no container restart required for either step — changes take effect
immediately on the next generation request.

## Backend commands

```bash
docker compose exec api ruff check .
docker compose exec api ruff format .
docker compose exec api mypy .
docker compose exec api pytest
```

## Frontend commands

```bash
docker compose exec web npm run lint
docker compose exec web npm run format
docker compose exec web npm run typecheck
docker compose exec web npm run test
docker compose exec web npm run build
```

## Worker commands

```bash
docker compose logs -f worker
docker compose restart worker
```

The worker has no interactive CLI in Sprint 1 — it only consumes the health-check queue
(see [BACKGROUND_JOB_ARCHITECTURE.md](../04-architecture/BACKGROUND_JOB_ARCHITECTURE.md)).

## Health-check commands

```bash
curl http://localhost:8000/health
```

Expected: `200 OK`. To confirm the worker is consuming, trigger a healthcheck job as an
Admin/Super Admin (`POST /system/jobs/healthcheck`), then check `docker compose logs
worker` for a `Processed healthcheck job <job_id>` line.

## Test commands

See [TEST_STRATEGY.md §Required test commands](../10-testing/TEST_STRATEGY.md#required-test-commands)
for the full authoritative list — this file only reiterates the ones you run locally day to
day (backend/frontend commands above).

## Lint and type-check commands

Covered under [§Backend commands](#backend-commands) and [§Frontend commands](#frontend-commands)
above. Both are also enforced by pre-commit hooks (`GRX-FOUND-001`) and CI (`GRX-DEVOPS-001`) —
running them locally before committing avoids a red CI run.

## Log inspection

```bash
docker compose logs -f api
docker compose logs -f web
docker compose logs -f worker
docker compose logs -f postgres redis rabbitmq
```

Structured backend logs are JSON — pipe through `jq` for readability, e.g.
`docker compose logs -f api | jq .` once structured logging is wired up (`GRX-FOUND-003`).

## Database access

```bash
docker compose exec postgres psql -U growixa -d growixa
```

Or from the host with any Postgres client at `localhost:5432` using the credentials in your
local `.env`.

## Redis inspection

```bash
docker compose exec redis redis-cli
```

Useful commands once rate limiting exists: `KEYS grx:ratelimit:*`, `TTL <key>`.

## RabbitMQ management access

Open `http://localhost:15672` in a browser (default local-only credentials `guest`/`guest`,
set in `.env` — never reused outside local dev). Shows queues, message rates, and the
`grx.system.healthcheck` queue's consumer status.

## Reset and cleanup commands

```bash
docker compose down
docker compose down -v   # also removes volumes: Postgres/Redis/RabbitMQ data is wiped
docker compose up --build --force-recreate
```

Use `down -v` when you want a genuinely clean slate (e.g. after a bad migration) — this
destroys local data only; it has no effect on any remote environment.

## Common troubleshooting

| Symptom | Likely cause | Fix |
|---|---|---|
| `api` fails to start, connection refused to Postgres | Postgres not ready yet | Compose should wait via healthcheck; if not, restart `api` after `postgres` is `healthy` |
| Alembic migration fails with "target database is not up to date" | Migrations applied out of order or a manual DB change | `alembic current` to check state; do not hand-edit the DB, fix via a new migration |
| Frontend can't reach backend | Wrong `NEXT_PUBLIC_API_URL` in `apps/web/.env.local` | Point it at `http://localhost:8000` (or the `api` service name if calling from inside another container) |
| Login works but session doesn't persist | Cookie domain/SameSite mismatch between `web` and `api` origins | Ensure both are served from origins that satisfy the configured `SameSite` policy locally (e.g. both on `localhost` with matching ports configuration) |
| RabbitMQ worker not consuming | Worker container crashed or wrong queue name | `docker compose logs worker`; confirm queue name matches `grx.system.healthcheck` |
| Port already in use | Another local process on 3000/8000/5432/6379/5672/15672 | Stop the conflicting process or override the port mapping in a local compose override file (not committed) |

## Safe handling of local credentials

- Never commit `.env` or `.env.local` — both are git-ignored.
- Never paste a real (non-local) secret into `.env.example`, code, commit messages, or chat
  with any coding agent.
- Local RabbitMQ/Postgres/Redis credentials in `.env.example` are placeholder development
  defaults, not meant to be reused anywhere reachable from the internet.
- If a local `.env` is ever accidentally staged (`git status` will show it if so — it
  shouldn't, since it's git-ignored, but verify before every commit per
  [AGENT_EXECUTION_RULES.md](../12-development/AGENT_EXECUTION_RULES.md)), unstage it and
  treat any secret in it as compromised if it was ever pushed.
