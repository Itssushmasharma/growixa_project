---
slug: "domain-verification"
category: "getting-started"
title: "Domain Verification, SPF & DKIM Setup"
excerpt: "Configure DNS records to achieve 99%+ email inbox delivery and protect your brand domain reputation."
icon: "🛡️"
readingTimeMinutes: 5
lastUpdated: "2026-08-17"
tags: ["dns", "spf", "dkim", "dmarc", "deliverability", "inbox"]
relatedSlugs: ["quickstart", "postmark-setup"]
---

## Why DNS Authentication is Critical

Major email providers like Gmail, Yahoo, and Outlook enforce strict SPF, DKIM, and DMARC authentication policies. Unauthenticated emails are frequently routed to the spam folder or rejected entirely.

## Required DNS Records

Add the following authentication records at your DNS host (Cloudflare, GoDaddy, Namecheap, Route53):

- **SPF (TXT)**: `v=spf1 include:spf.mtasv.net ~all` (when using Postmark) or your mail provider's SPF include record.
- **DKIM (CNAME or TXT)**: Add the DKIM public key records generated by your email delivery provider (such as Postmark or your mail provider).
- **DMARC (TXT)**: `v=DMARC1; p=none; rua=mailto:dmarc-reports@yourdomain.com` at `_dmarc.yourdomain.com` to monitor deliverability.
