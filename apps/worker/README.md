# Growixa Worker

Background job worker for Growixa. Sprint 1 scope is a connectivity skeleton only — it
consumes the `grx.system.healthcheck` queue, no business job types exist yet. See
[`docs/04-architecture/BACKGROUND_JOB_ARCHITECTURE.md`](../../docs/04-architecture/BACKGROUND_JOB_ARCHITECTURE.md).
